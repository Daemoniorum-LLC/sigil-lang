// TDD Test: Prefix ? option syntax
// Test ⎇ -> ?T works as Option<T>

λ maybe_get(cond: bool) -> ?i32 {
    ⎇ cond {
        42
    } ⎉ {
        null
    }
}

λ main() {
    ≔ result = maybe_get(true);
    println(result);
}
