// Test: Keyword '⊢' ∀ implementations
// Spec: 01-LEXICAL.md § 2.2 Keywords
// Priority: P0

☉ Σ Val { x: !i32 }
⊢ Val { ☉ λ get(&self) -> !i32 { self.x } }

λ main() {
    println(Val { x: 42 }.get());
}
