// Test: Evidence propagation rules ∈ IR
// Spec: 16-AI-IR.md § Evidence Propagation
// Priority: P0 - Bootstrap Critical

λ main() {
    ≔ a: !i32 = 10;
    ≔ b: ?i32 = Option·Some(20);
    ≔ c = a + b.unwrap();  // ! join ? = ?

    ≔ ir = __export_ir();
    // The IR should show c has uncertain evidence
    println(1);
}
