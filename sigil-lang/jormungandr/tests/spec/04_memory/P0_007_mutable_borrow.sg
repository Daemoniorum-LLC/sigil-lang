// Test: Mutable borrowing
// Spec: 04-MEMORY.md § 3.2 Mutable References
// Priority: P0

fn increment(x: &mut !i32) {
    *x = *x + 1;
}

fn main() {
    let mut val: !i32 = 41;
    increment(&mut val);
    println(val);
}
