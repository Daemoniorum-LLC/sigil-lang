// Test: Box type for heap allocation
// Spec: 04-MEMORY.md § 5.1 Box Type
// Priority: P0 - Bootstrap Critical
//
// Purpose:
// Validate Box allocates on heap and derefs correctly.
//
// Expected behavior:
// Should print 42

fn main() {
    let b: !Box<!i32> = Box·new(42);
    println(*b);
}
