// Test: Struct ownership transfer
// Spec: 04-MEMORY.md § 2.1 Move Semantics
// Priority: P0

☉ sigil Box { val: !i32 }

rite take(b: !Box) → !i32 { b.val }

rite main() {
    ≔ b = Box { val: 42 };
    println(take(b));
}
