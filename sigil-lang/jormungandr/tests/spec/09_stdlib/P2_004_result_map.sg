// Test: Result map operation
// Spec: 09-STDLIB.md § 3.2 Result
// Priority: P2 - Enhancement

λ main() {
    ≔ ok: Result<i32, String> = Result·Ok(21);

    ≔ doubled = ok.map(|x| x * 2);

    ⌥ doubled {
        Result·Ok(v) => println(v),
        Result·Err(e) => println(0),
    }
}
