// Test: Option unwrap
// Spec: 09-STDLIB.md § 3.1 Option
// Priority: P2 - Enhancement

fn main() {
    let opt: Option<i32> = Option::Some(42);
    let val: !i32 = opt.unwrap();
    println(val);
}
