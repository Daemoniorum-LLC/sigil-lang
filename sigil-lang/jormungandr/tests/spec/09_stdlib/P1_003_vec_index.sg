// Test: Vec indexing
// Spec: 09-STDLIB.md § 4.1 Vec
// Priority: P1 - Production Ready

λ main() {
    ≔ v: Vec<i32> = vec![10, 20, 30, 40];

    println(v[0]);
    println(v[1]);
    println(v[2]);
    println(v[3]);
}
