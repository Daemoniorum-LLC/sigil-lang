// Test: HashMap creation and insert
// Spec: 09-STDLIB.md § 4.3 HashMap
// Priority: P1 - Production Ready

fn main() {
    let mut map: HashMap<String, i32> = HashMap::new();

    map.insert("a", 1);
    map.insert("b", 2);

    println(map.len());
}
