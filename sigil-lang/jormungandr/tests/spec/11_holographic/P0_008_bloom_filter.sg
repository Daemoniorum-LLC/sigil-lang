// Test: Bloom filter
// Spec: 11-HOLOGRAPHIC.md § 3.1 Sketch Types
// Priority: P0 - Bootstrap Critical

fn main() {
    let bloom: BloomFilter<10000, 7> = BloomFilter·new();
    bloom.insert("hello");
    let maybe◊ = bloom|◊contains("hello");
    println(maybe◊);  // Should be true (possibly false positive)
}
