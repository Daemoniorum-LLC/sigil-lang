// Test: Count-Min Sketch
// Spec: 11-HOLOGRAPHIC.md § 3.1 Sketch Types
// Priority: P0 - Bootstrap Critical

fn main() {
    let cms: CountMinSketch<1024, 4> = CountMinSketch·new();
    cms.insert("item");
    cms.insert("item");
    cms.insert("item");
    let freq◊ = cms|◊frequency("item");
    println(freq◊);  // Should be ~3
}
