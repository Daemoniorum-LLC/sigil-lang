// Test: Dead code handling
// Spec: 18-COMPILER.md § 6.1 Optimizations
// Priority: P2 - Enhancement

λ unused() {
    println(0);
}

λ main() {
    // unused() is never called, but program should still work
    println(1);
}
