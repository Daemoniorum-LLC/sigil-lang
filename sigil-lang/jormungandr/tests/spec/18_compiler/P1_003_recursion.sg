// Test: Recursive function calls
// Spec: 18-COMPILER.md § 1.3 Recursion
// Priority: P1 - Standard Feature

λ factorial(n: !i32) -> !i32 {
    ⎇ n <= 1 {
        1
    } ⎉ {
        n * factorial(n - 1)
    }
}

λ main() {
    println(factorial(5));
    println(factorial(10));
}
