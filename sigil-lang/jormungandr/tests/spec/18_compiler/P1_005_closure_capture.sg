// Test: Closure captures outer variables
// Spec: 18-COMPILER.md § 2.2 Closures
// Priority: P1 - Standard Feature

fn main() {
    let x: !i32 = 10;
    let add_x = |y: !i32| -> !i32 { x + y };

    println(add_x(5));
    println(add_x(20));
}
