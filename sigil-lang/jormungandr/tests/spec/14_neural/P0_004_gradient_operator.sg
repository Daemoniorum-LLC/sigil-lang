// Test: Gradient operator (∇)
// Spec: 14-NEURAL.md § 3.1 The Gradient Operator
// Priority: P0 - Bootstrap Critical

fn main() {
    let x: Tensor<[]> = Tensor·from(3.0)|requires_grad(true);
    let y = x * x;  // y = x^2
    let grad = ∇(y, x);  // dy/dx = 2x = 6
    println(grad);
}
