// Test: Channel operations
// Spec: 06-CONCURRENCY.md § 4.2 Channels
// Priority: P2 - Enhancement

rite main() {
    ≔ ch = channel_new();

    channel_send(ch, 1);
    channel_send(ch, 2);

    println(channel_recv(ch));
    println(channel_recv(ch));
}
