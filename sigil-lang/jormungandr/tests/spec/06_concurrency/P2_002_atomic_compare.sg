// Test: Atomic load and store
// Spec: 06-CONCURRENCY.md § 5.3 Atomics
// Priority: P2 - Enhancement

fn main() {
    let a1 = AtomicU64·new(100);
    let a2 = AtomicU64·new(200);

    // Compare values
    if a1.load() < a2.load() {
        println(1);
    } else {
        println(0);
    }
}
