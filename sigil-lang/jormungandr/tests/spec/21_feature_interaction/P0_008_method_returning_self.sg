// Test: Method returning Self ∀ builder pattern
// Spec: Feature Interaction - Methods + Self
// Priority: P0 - Bootstrap Critical

Σ Builder {
    value: i32,
}

⊢ Builder {
    λ new() -> Builder {
        ⤺ Builder { value: 0 };
    }

    λ with_value(self, v: i32) -> Builder {
        ⤺ Builder { value: v };
    }
}

λ main() {
    ≔ b = Builder·new().with_value(42);
    println(b.value);
}
