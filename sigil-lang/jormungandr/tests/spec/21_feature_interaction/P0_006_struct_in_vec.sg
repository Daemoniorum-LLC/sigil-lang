// Test: Structs stored ∈ Vec
// Spec: Feature Interaction - Structs + Collections
// Priority: P0 - Bootstrap Critical

Σ Item {
    value: i32,
}

λ main() {
    ≔ Δ items: Vec<Item> = Vec·new();
    items.push(Item { value: 10 });
    items.push(Item { value: 20 });
    println(items.len());
    println(items[0].value);
}
