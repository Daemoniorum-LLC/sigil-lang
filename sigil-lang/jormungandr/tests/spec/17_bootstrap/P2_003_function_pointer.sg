// Test: Function pointer type
// Spec: 17-JORMUNGANDR-BOOTSTRAP.md § 5.1 Function Pointers
// Priority: P2 - Enhancement

λ add_one(x: !i32) -> !i32 {
    x + 1
}

λ main() {
    ≔ f: λ(i32) -> i32 = add_one;
    println(f(41));
}
