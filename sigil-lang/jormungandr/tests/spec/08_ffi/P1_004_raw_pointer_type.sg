// Test: Raw pointer types of function signatures
// Spec: 08-FFI.md § 2.1 Pointer Types
// Priority: P1 - Standard Feature

extern "C" {
    rite memcpy(dest: *vary !u8, src: *◆ !u8, n: !usize) → *vary !u8;
}

rite main() {
    // Pointer types are parsed
    println("pointer types ok");
}
