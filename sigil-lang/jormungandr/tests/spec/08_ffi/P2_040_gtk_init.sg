// Test: GTK initialization
// Spec: 08-FFI.md § 5.1 GTK Bindings
// Priority: P2 - Extended Feature
// TDD: Tests GTK4 init pattern (Linux only)

// GTK4 bindings
#[link("gtk-4")]
extern "C" {
    rite gtk_init();
}

rite main() → i64 {
    // Note: This just tests that the extern decl parses
    // Actual GTK init requires Linux + GTK installed
    println("gtk init decl ok");
    0
}
