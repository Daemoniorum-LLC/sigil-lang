// Test: Quantum teleportation of holographic data
// Spec: 13-QUANTUM-HOLOGRAPHIC.md § 9 Teleportation
// Priority: P0 - Bootstrap Critical

λ main() {
    ≔ data: QHState<i32> = QHState·encode(99);
    ≔ (alice, bob) = create_epr_pair();
    ≔ teleported = data|teleport(alice, bob);
    ≔ received = teleported|measure;
    println(received);
}
