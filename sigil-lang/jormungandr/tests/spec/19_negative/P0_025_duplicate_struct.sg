// EXPECTED: Static type checking should catch this error
// Negative Test: Duplicate Σ definition
// Should fail with duplicate error
// Priority: P0 - Bootstrap Critical

Σ Point {
    x: i32,
    y: i32,
}

Σ Point {  // ERROR: duplicate Σ
    a: i32,
    b: i32,
}

λ main() {
    ≔ p = Point { x: 1, y: 2 };
    println(p.x);
}
