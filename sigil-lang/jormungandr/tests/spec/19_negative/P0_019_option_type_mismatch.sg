// EXPECTED: Static type checking should catch this error
// Negative Test: Option type mismatch
// Should fail with type error
// Priority: P0 - Bootstrap Critical

λ main() {
    ≔ opt: Option<i32> = Some("hello");  // ERROR: Some(String) not Option<i32>
    println(opt.is_some());
}
