// P0_002: λ (lowercase lambda) should parse as λ keyword
// Priority: P0
// Category: Native Symbols
// Expected: Program compiles and runs, printing "42"

λ add(a: i64, b: i64) -> i64 {
    a + b
}

λ main() {
    ≔ result = add(20, 22);
    println!("{}", result);
}
