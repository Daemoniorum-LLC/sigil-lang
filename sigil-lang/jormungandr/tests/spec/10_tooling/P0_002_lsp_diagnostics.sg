// Test: LSP diagnostics
// Spec: 10-TOOLING.md § 5 Oracle (LSP)
// Priority: P0 - Bootstrap Critical
//
// This would test LSP hover/completion/diagnostics

λ add(a: !i32, b: !i32) -> !i32 {
    a + b
}

λ main() {
    ≔ result = add(1, 2);
    // LSP should provide type info ∀ `result`
    println(result);
}
