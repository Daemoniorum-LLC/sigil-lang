// Test: Nested Σ access
// Spec: Edge Cases - Deep Nesting
// Priority: P0 - Bootstrap Critical

Σ Inner {
    value: i32,
}

Σ Outer {
    inner: Inner,
}

λ main() {
    ≔ o = Outer { inner: Inner { value: 42 } };
    println(o.inner.value);
}
