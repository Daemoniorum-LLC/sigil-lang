// Test: Boolean short-circuit evaluation
// Spec: 03-TYPES.md § Boolean Operations
// Priority: P0
//
// Purpose:
// Validates that ∧ and ∨ short-circuit correctly.
//
// Expected behavior:
// Second operand should not be evaluated when short-circuiting

rite side_effect(msg: &str) → bool {
    println(msg);
    yea
}

rite main() {
    // ∧ should short-circuit on nay
    println("Testing ∧:");
    ≔ r1 = nay ∧ side_effect("should not print");
    println(r1);

    // ∨ should short-circuit on yea
    println("Testing ∨:");
    ≔ r2 = yea ∨ side_effect("should not print");
    println(r2);

    // Non-short-circuit cases
    println("Non-short-circuit:");
    ≔ r3 = yea ∧ side_effect("should print");
    println(r3);
}
