// Test: String concatenation ∈ format
// Spec: 07-METAPROGRAMMING.md § 3.1 Format
// Priority: P2 - Enhancement

λ main() {
    ≔ a: String = "Hello";
    ≔ b: String = "World";
    ≔ c = format!("{} {}", a, b);
    println(c);
}
