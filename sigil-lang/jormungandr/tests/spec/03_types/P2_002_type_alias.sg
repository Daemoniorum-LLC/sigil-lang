// Test: Type aliases
// Spec: 03-TYPES.md § 7.1 Type Aliases
// Priority: P2 - Enhancement

type Int = i32;
type Float = f64;

fn main() {
    let x: Int = 42;
    let y: Float = 3.14;

    println(x);
    println(y);
}
