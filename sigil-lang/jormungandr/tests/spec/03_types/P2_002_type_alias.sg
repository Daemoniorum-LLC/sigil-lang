// Test: Type aliases
// Spec: 03-TYPES.md § 7.1 Type Aliases
// Priority: P2 - Enhancement

type Int = i32;
type Float = f64;

λ main() {
    ≔ x: Int = 42;
    ≔ y: Float = 3.14;

    println(x);
    println(y);
}
