// Test: Generic Σ instantiation
// Spec: 03-TYPES.md § 6.1 Generics
// Priority: P2 - Enhancement

Σ Pair<T> {
    first: T,
    second: T,
}

λ main() {
    ≔ p: Pair<i32> = Pair { first: 10, second: 20 };
    println(p.first);
    println(p.second);
}
