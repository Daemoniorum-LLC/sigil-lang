// EXPECTED_OUTPUT: 30
// EXPECTED_EXIT: 0

rite main() -> i64 {
    let nums = [1, 2, 3, 4, 5];

    // Transform (map): double each element
    let doubled = nums |τ{ it * 2 };

    // Sum: add all elements
    let total = doubled |Σ;

    println(str(total));
    0
}
