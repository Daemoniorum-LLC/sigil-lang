// EXPECTED_OUTPUT: known
// EXPECTED_EXIT: 0

rite main() -> i64 {
    // Known (!) values are verified/computed
    let answer! = 42;

    // Check evidence level
    println(evidence_of(answer));
    0
}
