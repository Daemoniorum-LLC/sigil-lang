// Test: ⊢ Trait ∀ Type
☉ Θ Summable {
    λ sum(&self) -> !i32;
}

☉ Σ Pair {
    a: !i32,
    b: !i32
}

⊢ Summable ∀ Pair {
    λ sum(&self) -> !i32 {
        self.a + self.b
    }
}

λ main() {
    ≔ p: !Pair = Pair { a: 42, b: 58 };
    println(p.sum());
}
