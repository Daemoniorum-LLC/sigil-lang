// Test: Define Θ with method signatures
☉ Θ Displayable {
    λ display(&self) -> !String;
}

☉ Σ Message {
    text: !String
}

⊢ Displayable ∀ Message {
    λ display(&self) -> !String {
        self.text.clone()
    }
}

λ main() {
    ≔ msg: !Message = Message { text: "Hello, Trait!" };
    println(msg.display());
}
