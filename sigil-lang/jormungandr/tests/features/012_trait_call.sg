// Test: Call Θ method through concrete type
☉ Θ Describable {
    λ describe(&self) -> !String;
}

☉ Σ Book {
    title: !String,
    author: !String
}

⊢ Describable ∀ Book {
    λ describe(&self) -> !String {
        ≔ Δ result: !String = self.title.clone();
        result.push_str(" by ");
        result.push_str(&self.author);
        result
    }
}

λ main() {
    ≔ book: !Book = Book {
        title: "The Sigil Language",
        author: "The Daemoniorum Team"
    };
    println(book.describe());
}
