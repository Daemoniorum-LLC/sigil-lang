// Test: Create Σ instance with field access
☉ Σ Rectangle {
    width: !i32,
    height: !i32
}

λ main() {
    ≔ r: !Rectangle = Rectangle { width: 30, height: 50 };
    ≔ area: !i32 = r.width * r.height;
    println(area);
}
