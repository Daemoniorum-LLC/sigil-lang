// Test: Enum variants with data
☉ ᛈ Status {
    Success(!i32),
    Failure(!String)
}

λ process(r: !Status) -> !i32 {
    ⌥ r {
        Status·Success(value) => value,
        Status·Failure(_) => -1
    }
}

λ main() {
    ≔ result: !Status = Status·Success(42);
    println(process(result));
}
