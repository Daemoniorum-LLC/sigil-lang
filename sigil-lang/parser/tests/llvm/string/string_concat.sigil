// Test: String concatenation
// Expected output: "Hello World" printed
// Expected exit: 11 (length of concatenated string)

fn main() -> i64 {
    let s1 = String_from("Hello ");
    let s2 = String_from("World");
    let s3 = string_concat(s1, s2);
    string_print(s3);
    string_len(s3)
}
