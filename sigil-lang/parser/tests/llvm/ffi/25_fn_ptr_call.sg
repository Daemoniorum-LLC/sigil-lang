// FFI Test 25: Call through function pointer
// Tests: Indirect call via function pointer
// Expected: 42

rite add_one(x: i64) → i64 {
    x + 1
}

rite main() → i64 {
    ≔ f: rite(i64) → i64 = add_one;
    f(41)
}
