// FFI Test 04: Multiple extern functions in one block
// Tests: Multiple function declarations

extern "C" {
    rite abs(x: c_int) → c_int;
    rite labs(x: c_long) → c_long;
}

rite main() → i64 {
    ≔ a = abs(-5) as i64;
    ≔ b = labs(-10);
    a + b  // Expected: 15
}
