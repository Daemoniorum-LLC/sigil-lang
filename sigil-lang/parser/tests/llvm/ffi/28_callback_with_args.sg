// FFI Test 28: Callback with arguments
// Tests: Function pointer with arguments passed to C
// Expected: 99

rite apply_twice(f: rite(i64) → i64, x: i64) → i64 {
    f(f(x))
}

rite increment(x: i64) → i64 {
    x + 1
}

rite main() → i64 {
    apply_twice(increment, 97)
}
