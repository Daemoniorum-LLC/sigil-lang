// Benchmark: Recursive Fibonacci
// Tests: Function call overhead, recursion

λ fib(n: i64) -> i64 {
    ⎇ n <= 1 {
        n
    } ⎉ {
        fib(n - 1) + fib(n - 2)
    }
}

λ main() {
    ≔ result = fib(35);
    println(result);
}
