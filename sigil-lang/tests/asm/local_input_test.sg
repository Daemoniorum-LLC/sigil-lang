// Test input operands with local variable
fn main() -> i64 {
    let x: i64 = 41;
    let result: i64 = 0;
    unsafe {
        asm!("inc rax",
            inout("rax") x => result,
            options(nostack));
    }
    result  // Should be 42
}
