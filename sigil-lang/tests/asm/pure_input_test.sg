// Test pure input operand with local variable
fn main() -> i64 {
    let x: i64 = 5;
    let result: i64 = 0;
    unsafe {
        asm!("mov rax, rdi",
            in("rdi") x,
            out("rax") result,
            options(nostack));
    }
    result  // Should be 5
}
