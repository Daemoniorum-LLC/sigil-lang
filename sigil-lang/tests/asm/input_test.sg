// Test input operands with function parameters
λ add_one(x: i64) -> i64 {
    ≔ result: i64 = 0;
    unsafe {
        asm!("inc rax",
            inout("rax") x => result,
            options(nostack));
    }
    result
}

λ main() -> i64 {
    add_one(41)  // Should ⤺ 42
}
