// Automatic differentiation from README
fn square(x: f64) -> f64 {
    x * x
}

fn main() {
    // Gradient of x^2 at x=3 should be 6
    let derivative = grad(square, 3.0);
    println("Gradient of x^2 at 3:");
    println(derivative);
}
