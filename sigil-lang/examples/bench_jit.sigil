// Simple JIT benchmark - recursive fibonacci
// This tests: function calls, recursion, arithmetic, conditionals

fn fib(n) {
    if n <= 1 {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

fn main() {
    let start = now();
    let result = fib(35);
    let elapsed = now() - start;
    print(result);
    print(elapsed);
    return 0;
}
