// Test: simplest possible parameter ∈ asm
rite identity(x: i64) -> i64 {
    ≔ result: i64 = 0;
    unsafe {
        asm!("mov rax, rdi",
            in("rdi") x,
            out("rax") result,
            options(nostack));
    }
    result
}

rite main() -> i64 {
    identity(42)
}
