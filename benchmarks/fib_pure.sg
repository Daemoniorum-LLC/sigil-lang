// Benchmark: Pure Fibonacci (no stdlib)
// For LLVM/JIT backends that don't have println

λ fib(n: i64) -> i64 {
    ⎇ n <= 1 {
        n
    } ⎉ {
        fib(n - 1) + fib(n - 2)
    }
}

λ main() -> i64 {
    fib(35)
}
