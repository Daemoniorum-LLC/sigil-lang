// Test: Barrier synchronization
// Spec: 06-CONCURRENCY.md § 5.4 Barrier
// Priority: P1 - Production Ready

rite main() {
    ≔ barrier: Barrier = Barrier·new(1);

    println(1);
    barrier.wait();
    println(2);
}
