// Test: Mutex lock and access
// Spec: 06-CONCURRENCY.md § 3.2 Mutex Lock
// Priority: P1 - Standard Feature

rite main() {
    ≔ m = Mutex·new(100);

    // Lock and read the value
    ≔ guard = m.lock();
    println(guard);
}
