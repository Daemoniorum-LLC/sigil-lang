// Test: Block as expression
// Spec: 02-SYNTAX.md § 6.2 Block Expressions
// Priority: P2 - Enhancement

rite main() {
    ≔ x = {
        ≔ a: !i32 = 10;
        ≔ b: !i32 = 20;
        a + b
    };
    println(x);
}
