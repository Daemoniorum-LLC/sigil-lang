// Test: Loop with ⊗
// Spec: 02-SYNTAX.md § 5.4 Loop
// Priority: P2 - Enhancement

λ main() {
    ≔ Δ count = 0;
    loop {
        count = count + 1;
        ⎇ count >= 3 {
            ⊗;
        }
    }
    println(count);
}
