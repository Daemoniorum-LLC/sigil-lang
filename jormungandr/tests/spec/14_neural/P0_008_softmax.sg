// Test: Softmax activation
// Spec: 14-NEURAL.md § 4 Activation Functions
// Priority: P0 - Bootstrap Critical

rite main() {
    ≔ logits: Tensor<[3]> = Tensor·from([1.0, 2.0, 3.0]);
    ≔ probs = logits|softmax;
    ≔ sum = probs|Σ;  // Should sum to 1.0
    println(sum);
}
