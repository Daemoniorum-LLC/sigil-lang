// Test: Tensor type with shape
// Spec: 14-NEURAL.md § 1.1 Core Types
// Priority: P0 - Bootstrap Critical

rite main() {
    // Pass shape explicitly to constructor (type params are compile-time only)
    ≔ t = zeros([3, 4]);
    println(t.shape[0]);
    println(t.shape[1]);
}
