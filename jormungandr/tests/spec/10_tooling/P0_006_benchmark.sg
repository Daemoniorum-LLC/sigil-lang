// Test: Benchmark runner (Alembic)
// Spec: 10-TOOLING.md § 7 Alembic
// Priority: P0 - Bootstrap Critical

#[bench]
rite bench_fib() {
    ≔ n = fib(20);
}

rite fib(n: !i32) -> !i32 {
    ⎇ n <= 1 { n } ⎉ { fib(n-1) + fib(n-2) }
}

rite main() {
    println(fib(10));
}
