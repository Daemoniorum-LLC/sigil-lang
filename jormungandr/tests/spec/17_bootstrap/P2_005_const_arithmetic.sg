// Test: Const arithmetic expressions
// Spec: 17-BOOTSTRAP.md § 1.2 Const
// Priority: P2 - Enhancement

const VALUE: i32 = 10;

fn main() {
    let result: !i32 = VALUE * 2 + 5;
    println(result);
}
