// Test: Struct field access in C codegen
// Spec: 17-JORMUNGANDR-BOOTSTRAP.md § CG-001 Field Access
// Priority: P0
//
// Purpose:
// Validates that multiple field accesses generate correct C code.
//
// Expected behavior:
// Should compile and print 42 then 10

pub struct Point {
    x: !i32,
    y: !i32
}

fn main() {
    let p: !Point = Point { x: 42, y: 10 };
    println(p.x);
    println(p.y);
}
