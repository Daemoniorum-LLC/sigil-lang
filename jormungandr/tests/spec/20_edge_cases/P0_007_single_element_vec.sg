// Test: Single element vector
// Spec: Edge Cases - Collection Boundaries
// Priority: P0 - Bootstrap Critical

fn main() {
    let v: Vec<i32> = vec![42];
    println(v.len());
    println(v[0]);
}
