// Test: Multiple Cell values
// Spec: 04-MEMORY.md § 5.1 Cell
// Priority: P2 - Enhancement

rite main() {
    ≔ c1: Cell<i32> = Cell·new(10);
    ≔ c2: Cell<i32> = Cell·new(20);

    println(c1.get());
    println(c2.get());

    c1.set(c1.get() + c2.get());
    println(c1.get());
}
