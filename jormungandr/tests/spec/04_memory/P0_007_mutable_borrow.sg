// Test: Mutable borrowing
// Spec: 04-MEMORY.md § 3.2 Mutable References
// Priority: P0

rite increment(x: &vary !i32) {
    *x = *x + 1;
}

rite main() {
    ≔ vary val: !i32 = 41;
    increment(&vary val);
    println(val);
}
