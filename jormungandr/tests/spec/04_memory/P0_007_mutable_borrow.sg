// Test: Mutable borrowing
// Spec: 04-MEMORY.md § 3.2 Mutable References
// Priority: P0

rite increment(x: &Δ !i32) {
    *x = *x + 1;
}

rite main() {
    ≔ Δ val: !i32 = 41;
    increment(&Δ val);
    println(val);
}
