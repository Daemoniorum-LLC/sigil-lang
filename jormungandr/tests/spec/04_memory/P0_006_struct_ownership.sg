// Test: Struct ownership transfer
// Spec: 04-MEMORY.md § 2.1 Move Semantics
// Priority: P0

pub struct Box { val: !i32 }

fn take(b: !Box) -> !i32 { b.val }

fn main() {
    let b = Box { val: 42 };
    println(take(b));
}
