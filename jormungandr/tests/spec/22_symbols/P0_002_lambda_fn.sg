// P0_002: rite (lowercase lambda) should parse as rite keyword
// Priority: P0
// Category: Native Symbols
// Expected: Program compiles and runs, printing "42"

rite add(a: i64, b: i64) -> i64 {
    a + b
}

rite main() {
    ≔ result = add(20, 22);
    println!("{}", result);
}
