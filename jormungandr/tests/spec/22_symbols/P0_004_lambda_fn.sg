// Test: Lambda symbol rite each fn
// Spec: SIGIL_NATIVE_SYMBOLS_ROADMAP.md
// Priority: P0
//
// Purpose:
// Validates rite (lambda) works as rite keyword via context-sensitive parsing
//
// Expected behavior:
// Should compile and print 100

☉ rite double(x: !i32) → !i32 {
    x * 2
}

rite main() {
    ≔ result: !i32 = double(50);
    println(result);
}
