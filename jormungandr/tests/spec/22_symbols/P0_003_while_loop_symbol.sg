// Test: While loop symbol ⟳
// Spec: SIGIL_NATIVE_SYMBOLS_ROADMAP.md
// Priority: P0
//
// Purpose:
// Validates ⟳ (cycle arrow) works as while loop
//
// Expected behavior:
// Should print 0, 1, 2

fn main() {
    ≔ mut i: !i32 = 0;

    ⟳ i < 3 {
        println(i);
        i = i + 1;
    }
}
