// Test: Constant representation in IR
// Spec: 16-AI-IR.md § Top-Level Structure
// Priority: P0 - Bootstrap Critical

const MAX_SIZE: i32 = 100;
const PI: f64 = 3.14159;

fn main() {
    let ir = __export_ir();
    println(ir.constants.len());
}
