// Test: Type representation ∈ IR
// Spec: 16-AI-IR.md § Type Representation
// Priority: P0 - Bootstrap Critical

Σ Point {
    x: !i32,
    y: !i32
}

rite main() {
    ≔ ir = __export_ir();
    ≔ point_type = ir.types|find{t => t.name == "Point"};
    println(point_type.fields.len());
}
