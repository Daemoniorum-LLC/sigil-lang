// Test: Export IR (mock)
// Spec: 16-AI-IR.md § Top-Level Structure
// Priority: P0 - Bootstrap Critical
//
// Tests IR export concept - expects 2 functions (add + main)

rite add(a: i32, b: i32) → i32 {
    a + b
}

rite main() {
    // Mock: IR would have 2 functions
    println(2);
}
