// Test: Source span information ∈ IR
// Spec: 16-AI-IR.md § Source Mapping
// Priority: P0 - Bootstrap Critical

rite main() {
    ≔ x = 42;
    ≔ ir = __export_ir();
    ≔ main_fn = ir.functions|find{f => f.name == "main"};
    // Should have source span info
    println(main_fn.span.start.line);
}
