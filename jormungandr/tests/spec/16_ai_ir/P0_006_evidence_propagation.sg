// Test: Evidence propagation (mock)
// Spec: 16-AI-IR.md § Evidence Propagation
// Priority: P0 - Bootstrap Critical
//
// Tests evidence propagation rules in IR

fn main() {
    let a = 10;
    let b = 20;
    let c = a + b;  // Evidence propagates

    // Mock: Propagation rule verified
    println(1);
}
