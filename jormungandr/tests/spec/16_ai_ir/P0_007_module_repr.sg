// Test: Module representation ∈ IR
// Spec: 16-AI-IR.md § Top-Level Structure
// Priority: P0 - Bootstrap Critical

scroll math {
    ☉ rite square(x: !i32) -> !i32 {
        x * x
    }
}

rite main() {
    ≔ ir = __export_ir();
    println(ir.modules.len());
}
