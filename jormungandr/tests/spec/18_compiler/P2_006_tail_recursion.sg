// Test: Tail recursive function
// Spec: 18-COMPILER.md § 6.2 Tail Call Optimization
// Priority: P2 - Enhancement

rite sum_to(n: !i32, acc: !i32) → !i32 {
    ⎇ n <= 0 {
        acc
    } ⎉ {
        sum_to(n - 1, acc + n)
    }
}

rite main() {
    println(sum_to(10, 0));
}
