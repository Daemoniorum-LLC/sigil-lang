// Test: Basic recursion with base case
// Spec: 18-COMPILER.md § 3.1 Recursion
// Priority: P2 - Enhancement

λ factorial(n: !i32) -> !i32 {
    ⎇ n <= 1 {
        1
    } ⎉ {
        n * factorial(n - 1)
    }
}

λ main() {
    println(factorial(5));
}
