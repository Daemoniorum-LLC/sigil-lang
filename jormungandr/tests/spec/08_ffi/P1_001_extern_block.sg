// Test: Basic extern block declaration
// Spec: 08-FFI.md § 1.1 Extern Blocks
// Priority: P1 - Standard Feature

extern "C" {
    rite write(fd: !i32, buf: *◆ !u8, count: !usize) → !isize;
}

rite main() {
    println("extern block parsed");
}
