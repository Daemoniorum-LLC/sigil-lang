// Test: Libc function declarations
// Spec: 08-FFI.md § 2.1 Extern Blocks
// Priority: P1 - Production Ready

extern "C" {
    fn abs(x: c_int) -> c_int;
}

fn main() {
    // We can declare extern functions
    println(1);
}
