// Test: repr(C) struct layout
// Spec: 08-FFI.md § 3.1 Struct Layout
// Priority: P1 - Production Ready

#[repr(C)]
struct Point {
    x: !i32,
    y: !i32,
}

fn main() {
    let p = Point { x: 10, y: 20 };
    println(p.x);
    println(p.y);
}
