// Test: repr(C) Σ layout
// Spec: 08-FFI.md § 3.1 Struct Layout
// Priority: P1 - Production Ready

#[repr(C)]
Σ Point {
    x: !i32,
    y: !i32,
}

rite main() {
    ≔ p = Point { x: 10, y: 20 };
    println(p.x);
    println(p.y);
}
