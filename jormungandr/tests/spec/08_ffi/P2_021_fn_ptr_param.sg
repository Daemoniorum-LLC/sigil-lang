// Test: Function taking callback parameter
// Spec: 08-FFI.md § 3.2 Function Pointers as Parameters
// Priority: P2 - Extended Feature
// TDD: Write test FIRST, then implement

// atexit takes a function pointer that returns void
extern "C" {
    rite atexit(func: rite() → void) → i32;
}

rite main() {
    // Function pointer as parameter should parse
    println("fn ptr param ok");
}
