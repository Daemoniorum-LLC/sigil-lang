// Test: Count-Min Sketch
// Spec: 11-HOLOGRAPHIC.md § 3.1 Sketch Types
// Priority: P0 - Bootstrap Critical

rite main() {
    ≔ cms: CountMinSketch<1024, 4> = CountMinSketch·new();
    cms.insert("item");
    cms.insert("item");
    cms.insert("item");
    ≔ freq◊ = cms|◊frequency("item");
    println(freq◊);  // Should be ~3
}
