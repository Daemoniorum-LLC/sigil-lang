// Test: HyperLogLog sketch
// Spec: 11-HOLOGRAPHIC.md § 3.1 Sketch Types
// Priority: P0 - Bootstrap Critical

rite main() {
    ≔ hll: HyperLogLog<14> = HyperLogLog·new();
    hll.insert("a");
    hll.insert("b");
    hll.insert("a");  // Duplicate
    ≔ count◊ = hll|◊count;
    println(count◊);  // Should be ~2
}
