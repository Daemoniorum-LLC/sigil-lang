// Test: Superposition type
// Spec: 11-HOLOGRAPHIC.md § 5.1 Quantum-Inspired Semantics
// Priority: P0 - Bootstrap Critical

fn main() {
    let sup: Superposition<i32> = uniform([1, 2, 3]);
    let observed! = sup|observe;  // Collapse to definite value
    println(observed!);
}
