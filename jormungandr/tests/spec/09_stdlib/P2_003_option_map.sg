// Test: Option map operation
// Spec: 09-STDLIB.md § 3.2 Option
// Priority: P2 - Enhancement

λ main() {
    ≔ some: Option<i32> = Option·Some(21);

    ≔ doubled = some.map(|x| x * 2);

    ⌥ doubled {
        Option·Some(v) => println(v),
        Option·None => println(0),
    }
}
