// Test: Option unwrap_or
// Spec: 09-STDLIB.md § 3.2 Option
// Priority: P1 - Production Ready

rite main() {
    ≔ some: Option<i32> = Option·Some(42);
    ≔ none: Option<i32> = Option·None;

    println(some.unwrap_or(0));
    println(none.unwrap_or(99));
}
