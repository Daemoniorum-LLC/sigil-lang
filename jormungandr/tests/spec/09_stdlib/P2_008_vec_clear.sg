// Test: Vec clear operation
// Spec: 09-STDLIB.md § 4.1 Vec
// Priority: P2 - Enhancement

rite main() {
    ≔ vary v: Vec<i32> = vec![1, 2, 3, 4, 5];
    println(v.len());

    v.clear();
    println(v.len());

    ⎇ v.is_empty() {
        println(1);
    } ⎉ {
        println(0);
    }
}
