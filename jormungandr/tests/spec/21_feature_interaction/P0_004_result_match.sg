// Test: Result with pattern matching
// Spec: Feature Interaction - Result + Match
// Priority: P0 - Bootstrap Critical

rite main() {
    ≔ ok_val: Result<i32, String> = Ok(100);
    ≔ result = ⌥ ok_val {
        Ok(x) => x,
        Err(_) => -1,
    };
    println(result);
}
