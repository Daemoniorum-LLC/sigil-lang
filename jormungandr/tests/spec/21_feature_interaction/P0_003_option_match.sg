// Test: Option with pattern matching
// Spec: Feature Interaction - Option + Match
// Priority: P0 - Bootstrap Critical

λ main() {
    ≔ some_val: Option<i32> = Some(42);
    ≔ result = ⌥ some_val {
        Some(x) => x,
        None => 0,
    };
    println(result);
}
