// Test: HTTP request with custom headers
// Spec: 15-PROTOCOLS.md § 4 HTTP Support
// Priority: P1 - Production Ready
//
// Tests http_request with custom headers

rite main() {
    // Test url_encode function
    ≔ encoded = url_encode("hello world");
    ⎇ encoded == "hello%20world" {
        println("OK");
    } ⎉ {
        println(encoded);
    }
}
