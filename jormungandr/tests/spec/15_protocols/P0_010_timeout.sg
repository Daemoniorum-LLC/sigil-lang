// Test: Protocol timeout (mock)
// Spec: 15-PROTOCOLS.md § 2 Common Abstractions
// Priority: P0 - Bootstrap Critical
//
// Tests timeout handling concept with mock

rite mock_request_with_timeout() → i32 {
    200  // Simulates successful response status
}

rite main() {
    ≔ status = mock_request_with_timeout();
    println(status);
}
