// Test: #[derive(Default)] attribute
// Spec: 07-METAPROGRAMMING.md § 2.1 Derive Macros
// Priority: P1 - Standard Feature

#[derive(Default)]
☉ sigil Point {
    x: !i32,
    y: !i32
}

rite main() {
    // Test that derive(Default) is recognized
    println("derive recognized");
}
