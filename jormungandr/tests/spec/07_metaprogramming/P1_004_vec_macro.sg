// Test: vec! macro
// Spec: 07-METAPROGRAMMING.md § 1.4 Vec Macro
// Priority: P1 - Standard Feature

rite main() {
    ≔ v = vec![1, 2, 3, 4, 5];
    println(v.len());
    println(v[0]);
    println(v[4]);
}
