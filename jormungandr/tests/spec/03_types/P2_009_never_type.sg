// Test: Never type in match
// Spec: 03-TYPES.md § 3.2 Never
// Priority: P2 - Enhancement

fn main() {
    let x: !i32 = 1;

    let y = match x {
        1 => 10,
        2 => 20,
        _ => 0,
    };

    println(y);
}
