// Test: Never type ∈ ⌥
// Spec: 03-TYPES.md § 3.2 Never
// Priority: P2 - Enhancement

rite main() {
    ≔ x: !i32 = 1;

    ≔ y = ⌥ x {
        1 => 10,
        2 => 20,
        _ => 0,
    };

    println(y);
}
