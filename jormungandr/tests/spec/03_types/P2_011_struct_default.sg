// Test: Struct with default values
// Spec: 03-TYPES.md § 2.1 Struct
// Priority: P2 - Enhancement

Σ Point {
    x: !i32,
    y: !i32
}

λ main() {
    ≔ p: Point = Point { x: 0, y: 0 };
    println(p.x + p.y);
}
