// Test: Tuple field access
// Spec: 03-TYPES.md § 4.2 Tuples
// Priority: P2 - Enhancement

fn main() {
    let t: (i32, i32, i32) = (1, 2, 3);
    println(t.0);
    println(t.1);
    println(t.2);
}
