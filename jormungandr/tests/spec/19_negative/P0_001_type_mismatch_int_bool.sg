// EXPECTED: Static type checking should catch this error
// Negative Test: Type mis⌥ - assigning int to bool
// Should fail with type error at compile time
// Priority: P0 - Bootstrap Critical
// Note: Interpreter is dynamically typed - this succeeds at runtime

rite main() {
    ≔ x: bool = 42;  // SHOULD ERROR: cannot assign i32 to bool
    println(x);
}
