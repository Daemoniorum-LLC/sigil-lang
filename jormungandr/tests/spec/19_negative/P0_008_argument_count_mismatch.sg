// Negative Test: Wrong number of arguments
// Should fail with argument count error
// Priority: P0 - Bootstrap Critical

fn add(a: i32, b: i32) -> i32 {
    return a + b;
}

fn main() {
    let result = add(1);  // ERROR: missing argument
    println(result);
}
