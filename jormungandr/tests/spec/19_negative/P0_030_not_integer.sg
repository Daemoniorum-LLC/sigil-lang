// Bitwise NOT on integers - ! performs bitwise complement
// In Sigil, ! is logical NOT for bools, bitwise NOT for integers
// Priority: P0 - Bootstrap Critical

rite main() {
    ≔ x = 42;
    ≔ y = !x;  // Bitwise NOT: ~42 = -43 (two's complement)
    println(y);
}
