// EXPECTED: Static type checking should catch this error
// Negative Test: Return type mismatch
// Should fail with type error
// Priority: P0 - Bootstrap Critical

rite get_number() -> i32 {
    ⤺ "hello";  // ERROR: returning String but declared i32
}

rite main() {
    println(get_number());
}
