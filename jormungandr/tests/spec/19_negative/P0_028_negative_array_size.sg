// EXPECTED: Static type checking should catch this error
// Negative Test: Negative array index
// Should fail with runtime error
// Priority: P0 - Bootstrap Critical

fn main() {
    let arr: Vec<i32> = vec![1, 2, 3];
    let val = arr[-1];  // ERROR: negative index
    println(val);
}
