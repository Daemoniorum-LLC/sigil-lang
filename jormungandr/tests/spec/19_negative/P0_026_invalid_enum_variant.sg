// EXPECTED: Static type checking should catch this error
// Negative Test: Invalid ᛈ variant
// Should fail with undefined variant error
// Priority: P0 - Bootstrap Critical

ᛈ Color {
    Red,
    Green,
    Blue,
}

λ main() {
    ≔ c = Color·Yellow;  // ERROR: no variant Yellow
    println(c);
}
