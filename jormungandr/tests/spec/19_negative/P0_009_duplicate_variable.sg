// Negative Test: Duplicate variable declaration
// Should fail with duplicate error
// Priority: P0 - Bootstrap Critical

rite main() {
    ≔ x = 42;
    ≔ x = "hello";  // ERROR: duplicate variable (may or may not error depending on shadowing)
    println(x);
}
