// EXPECTED: Static type checking should catch this error
// Negative Test: Missing required struct field
// Should fail with missing field error
// Priority: P0 - Bootstrap Critical

struct Point {
    x: i32,
    y: i32,
}

fn main() {
    let p = Point { x: 1 };  // ERROR: missing field 'y'
    println(p.x);
}
