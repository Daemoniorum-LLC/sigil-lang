// Negative Test: Invalid binary operation
// Should fail with type error
// Priority: P0 - Bootstrap Critical

rite main() {
    ≔ result = "hello" - 5;  // ERROR: cannot subtract from string
    println(result);
}
