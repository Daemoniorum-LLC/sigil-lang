// Negative Test: Cannot negate bool with -
// Should fail with type error
// Priority: P0 - Bootstrap Critical

rite main() {
    ≔ x = yea;
    ≔ y = -x;  // ERROR: cannot negate bool
    println(y);
}
