// EXPECTED: Static type checking should catch this error
// Negative Test: Duplicate sigil definition
// Should fail with duplicate error
// Priority: P0 - Bootstrap Critical

sigil Point {
    x: i32,
    y: i32,
}

sigil Point {  // ERROR: duplicate struct
    a: i32,
    b: i32,
}

rite main() {
    ≔ p = Point { x: 1, y: 2 };
    println(p.x);
}
