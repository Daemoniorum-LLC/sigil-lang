// Generated by Sigil Rust Codegen
// Edition: 2021

fn get_number() -> impl Clone + std::fmt::Debug {
    42
}

fn get_display() -> impl Clone + Copy + std::fmt::Display {
    42i32
}

fn make_range(n: i32) -> impl Iterator<Item = i32> {
    0..n
}

fn make_adder(n: i32) -> impl Fn(i32) -> i32 {
    move |x| x + n
}

fn make_counter() -> impl FnMut() -> i32 {
    let mut count = 0;
    move || {
        count = count + 1;
        count
    }
}

pub fn public_impl() -> impl Clone {
    "hello"
}

fn wrap_value<T: Clone>(x: T) -> impl Clone {
    x
}

fn main() {
    let num = get_number();
    println!("Number: {:?}", num);
    let disp = get_display();
    println!("Display: {}", disp);
    let mut range_iter = make_range(5);
    let sum: i32 = range_iter.fold(0, |a, b| a + b);
    println!("Range sum: {}", sum);
    let add_five = make_adder(5);
    println!("5 + 10 = {}", add_five(10));
    let mut counter = make_counter();
    println!("Count: {}", counter());
    println!("Count: {}", counter());
}

