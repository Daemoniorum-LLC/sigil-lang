// Test: String contains method
fn main() {
    let s: !String = "Hello, World!";

    if s.contains("World") {
        println("found");
    }

    if !s.contains("Goodbye") {
        println("not found");
    }
}
