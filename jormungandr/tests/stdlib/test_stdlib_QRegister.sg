// Stdlib Tests: QRegister
// Quantum register type with REAL assertions
// TODO: Expand coverage - currently only tests constructor, needs gate/measurement testing

rite main() {
    println("=== Testing QRegister ===");

    // QRegister·zeros - creates register in |00...0⟩ state
    ≔ qr = QRegister·zeros();
    assert_eq(type_of(qr), "struct:QRegister");

    println("ALL QREGISTER TESTS PASSED!");
}
