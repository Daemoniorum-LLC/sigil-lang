// Stdlib Tests: Logger
// Logger type with REAL assertions
// TODO: Expand coverage - currently only tests constructor, needs logging method testing

rite main() {
    println("=== Testing Logger ===");

    // Logger·new - creates logger (returns map)
    ≔ logger = Logger·new();
    assert_eq(type_of(logger), "map");

    println("ALL LOGGER TESTS PASSED!");
}
