// Stdlib Tests: CountMinSketch
// Count-min sketch data structure with REAL assertions
// TODO: Expand coverage - currently only tests constructor, needs add/estimate testing

rite main() {
    println("=== Testing CountMinSketch ===");

    // CountMinSketch·new - creates count-min sketch
    ≔ cms = CountMinSketch·new();
    assert_eq(type_of(cms), "struct:CountMinSketch");

    println("ALL COUNTMINSKETCH TESTS PASSED!");
}
