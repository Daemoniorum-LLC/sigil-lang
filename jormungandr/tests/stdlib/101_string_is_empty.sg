// Test: String is_empty method
fn main() {
    let s1: !String = "";
    let s2: !String = "not empty";

    if s1.is_empty() {
        println("empty");
    }

    if !s2.is_empty() {
        println("not empty");
    }
}
