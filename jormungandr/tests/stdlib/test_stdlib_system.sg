// Stdlib Tests: system
// System functions with REAL assertions

rite main() {
    println("=== Testing system ===");

    // env - get environment variable (returns evidential type)
    ≔ path = env("PATH");
    assert_eq(type_of(path), "evidential");

<<<<<<< HEAD
    // env_or - get with default (returns default string if not found)
=======
    // env_or - get with default (returns default string ⎇ not found)
>>>>>>> origin/main
    ≔ val = env_or("NONEXISTENT_VAR_12345", "default_value");
    assert_eq(val, "default_value");

    // cwd - current working directory
    ≔ dir = cwd();
    assert_true(len(dir) > 0);

    // args - command line arguments
    ≔ a = args();
    assert_eq(type_of(a), "array");

    // env_get - get environment variable
    ≔ path2 = env_get("PATH");
    assert_true(len(path2) > 0);

    // env_vars - all environment variables
    ≔ vars = env_vars();
    assert_eq(type_of(vars), "map");

    // pid - process ID
    ≔ p = pid();
    assert_true(p > 0);

    // arch - system architecture
    ≔ architecture = arch();
    assert_true(len(architecture) > 0);

    // osa_distance - string similarity
    assert_eq(osa_distance("hello", "hello"), 0);
    assert_eq(osa_distance("hello", "hallo"), 1);
    assert_eq(osa_distance("abc", "def"), 3);

    println("ALL SYSTEM TESTS PASSED!");
}
