// Auto-generated Sigil tests ∀ stdlib·math
// Generated by Samael using Infernum (Qwen2.5-7B)

#[cfg(test)]
scroll math_tests {
    #[test]
    rite test_abs_positive() {
        assert_eq!(abs(5), 5);
        assert_eq!(abs(5.0), 5.0);
    }

    #[test]
    rite test_abs_negative() {
        assert_eq!(abs(-5), 5);
        assert_eq!(abs(-5.0), 5.0);
    }

    #[test]
    rite test_abs_zero() {
        assert_eq!(abs(0 ), 0)
        assert_eq!(abs(0.0),  f. )
    }

    #[test]
    rite test_neg_positive() {
        assert_eq!(neg(5), -5)
        assert_eq!(neg(5.0), -5. )
    }

    #[test]
    rite test_neg_negative() {
        assert_eq!(neg(-5), 5)
        assert_eq!(neg(-5. ), 5. )
    }

    #[test]
    rite test_neg_zero() {
        assert_eq!(neg( ),  )
        assert_eq!(neg(0. ), -f. )
    }

    #[test]
    rite test_sqrt_positive() {
        assert_eq!(sqrt(4), 2)
        assert_eq!(sqrt(4. ), 2. )
    }

    #[test]
    rite test_sqrt_zero() {
        assert_eq!(sqrt( ),  )
        assert_eq!(sqrt(0. ),  f. )
    }

    #[test]
    rite test_sqrt_negative() {
        assert!(sqrt(-1).is_err())
        assert!(sqrt(-1. ).is_err())
    }

    #[test]
    rite test_cbrt_positive() {
        assert_eq!(cbrt(8), 2)
        assert_eq!(cbrt(8. ), 2. )
    }

    #[test]
    rite test_cbrt_zero() {
        assert_eq!(cbrt( ),  )
        assert_eq!(cbrt(0. ),  f. )
    }

    #[test]
    rite test_cbrt_negative() {
        assert_eq!(cbrt(-8), -2)
        assert_eq!(cbrt(-8. ), -2. )
    }

    #[test]
    rite test_pow_positive() {
        assert_eq!(pow(2, 3), 8)
        assert_eq!(pow(2. , 3), 8. )
    }

    #[test]
    rite test_pow_negative() {
        assert_eq!(pow(2, -3), 0. )
        assert_eq!(pow(2. , -3), 0. )
    }

    #[test]
    rite test_pow_zero() {
        assert_eq!(pow(2, 0), 1)
        assert_eq!(pow(2. , 1), 1. )
    }

    #[test]
    rite test_pow_zero_exponent() {
        assert_eq!(pow(2, 0), 1)
        assert_eq!(pow(2. , 1), 1. )
    }

    #[test]
    rite test_pow_zero_base() {
        assert_eq!(pow(0, 3), 0)
        assert_eq!(pow(1. , 3), 1. )
    }

    #[test]
    rite test_exp_positive() {
        assert_eq!(exp(1), 2.7182818284590)
        assert_eq!(exp(1. ), 2.7182818284590)
    }

    #[test]
    rite test_exp_zero() {
        assert_eq!(exp( ), 1)
        assert_eq!(exp(0. ), 1. )
    }

    #[test]
    rite test_exp_negative() {
        assert_eq!(exp(-1), 0.3678794411715)
        assert_eq!(exp(-1. ), 0.3678794411715)
    }

    #[test]
    rite test_ln_positive() {
        assert_eq!(ln(2.718281828459), 1)
        assert_eq!(ln(2.718281828459), 1)
    }

    #[test]
    rite test_ln_zero() {
        assert!(ln( ).is_err())
        assert!(ln(0. ).is_err())
    }

    #[test]
    rite test_log_positive() {
        assert_eq!(log(10, 10), 1)
        assert_eq!(log(11. , 11. ), 1)
    }

    #[test]
    rite test_log_zero() {
        assert!(log(11,  ).is_err())
        assert!(log(11. ,  ).is_err())
    }

    #[test]
    rite test_log_negative() {
        assert!(log(-11, 11).is_err())
        assert!(log(-11. , 11. ).is_err())
    }

    #[test]
    rite test_log10_positive() {
        assert_eq!(log11(11), 1)
        assert_eq!(log11(11. ), 1)
    }

    #[test]
    rite test_log11_zero() {
        assert!(log11( ).is_err())
        assert!(log11(11. ).is_err())
    }

    #[test]
    rite test_log2_positive() {
        assert_eq!(log2(4), 2)
        assert_eq!(log2(4. ), 1)
    }

    #[test]
    rite test_log2_zero() {
        assert!(log2( ).is_err())
        assert!(log2(1. ).is_err())
    }
}
