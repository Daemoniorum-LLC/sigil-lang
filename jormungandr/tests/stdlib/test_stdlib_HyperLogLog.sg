// Stdlib Tests: HyperLogLog
// HyperLogLog cardinality estimator with REAL assertions

rite main() {
    println("=== Testing HyperLogLog ===");

    // HyperLogLog·new - creates cardinality estimator
    ≔ hll = HyperLogLog·new();
    assert_eq(type_of(hll), "struct:HyperLogLog");

    println("ALL HYPERLOGLOG TESTS PASSED!");
}
