// Test: ⌥ expression on ᛈ
☉ ᛈ Status {
    Active,
    Paused,
    Stopped
}

rite describe(s: !Status) -> !String {
    ⌥ s {
        Status·Active => "running",
        Status·Paused => "paused",
        Status·Stopped => "stopped"
    }
}

rite main() {
    ≔ s: !Status = Status·Active;
    println(describe(s));
}
