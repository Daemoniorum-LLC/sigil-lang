// Test: Create sigil instance with field access
☉ sigil Rectangle {
    width: !i32,
    height: !i32
}

rite main() {
    ≔ r: !Rectangle = Rectangle { width: 30, height: 50 };
    ≔ area: !i32 = r.width * r.height;
    println(area);
}
