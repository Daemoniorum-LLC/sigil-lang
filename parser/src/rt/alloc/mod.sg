// rt/alloc/mod.sg - Memory Allocation
//
// Provides memory allocation without malloc/free from libc.
// Uses mmap syscall for memory acquisition.

pub mod arena;
pub mod global;

pub use arena·Arena;
pub use global·{init_allocator, sigil_alloc, sigil_free, sigil_realloc};
