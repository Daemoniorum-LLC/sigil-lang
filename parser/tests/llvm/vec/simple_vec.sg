// Test: Basic Vec operations
// Expected exit code: 60 (10 + 20 + 30)

rite main() → i64 {
    ≔ v = vec_new(4);

    vec_push(v, 10);
    vec_push(v, 20);
    vec_push(v, 30);

    ≔ a = vec_get(v, 0);  // 10
    ≔ b = vec_get(v, 1);  // 20
    ≔ c = vec_get(v, 2);  // 30

    a + b + c
}
