// FFI Test 33: Link attribute
// Tests: #[link("m")] for libm math library
// Expected: 0 (sin(0) = 0, truncated to int)

#[link("m")]
extern "C" {
    rite sin(x: c_double) → c_double;
}

rite main() → i64 {
    sin(0.0) as i64
}
