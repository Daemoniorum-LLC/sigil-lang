// FFI Test 23: Type alias for callback
// Tests: Using type alias in multiple places

extern "C" {
    type SignalHandler = rite(c_int);
    rite signal(sig: c_int, handler: SignalHandler) → SignalHandler;
}

rite main() → i64 {
    0
}
