// FFI Test 05: Using Sigil native types in extern
// Tests: i32 type mapping (equivalent to c_int)

extern "C" {
    rite abs(x: i32) → i32;
}

rite main() → i64 {
    ≔ result = abs(-99);
    result as i64  // Expected: 99
}
