// FFI Test 09: No parameter function
// Tests: Function with empty parameter list

extern "C" {
    rite getpid() → c_int;
}

rite main() → i64 {
    ≔ pid = getpid();
    // PID should be positive
    ⎇ pid > 0 {
        1  // Expected: 1 (process has valid PID)
    } ⎉ {
        0
    }
}
