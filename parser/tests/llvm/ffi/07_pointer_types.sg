// FFI Test 07: Pointer types
// Tests: *c_char pointer type with strlen

extern "C" {
    rite strlen(s: *c_char) → size_t;
}

rite main() → i64 {
    // Note: This tests pointer type declaration compiles
    // Actual string passing requires string interop
    0  // Expected: 0 (compilation test)
}
