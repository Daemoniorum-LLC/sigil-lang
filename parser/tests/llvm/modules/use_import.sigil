// Test: Use imports with module paths
// Expected output: 100

mod math {
    pub fn multiply(a: i64, b: i64) -> i64 {
        a * b
    }

    pub fn double(x: i64) -> i64 {
        multiply(x, 2)
    }
}

use math::double;

fn main() -> i64 {
    double(50)
}
