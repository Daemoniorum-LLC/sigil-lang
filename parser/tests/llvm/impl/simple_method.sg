// Test: Simple struct method
// Expected exit code: 30

Σ Point {
    x: i64,
    y: i64,
}

⊢ Point {
    rite sum(self) → i64 {
        self.x + self.y
    }
}

rite main() → i64 {
    ≔ p = Point { x: 10, y: 20 };
    p.sum()
}
