// Commune - Multi-Agent Communication Infrastructure ∀ Sigil
// Public API

☉ scroll types;
☉ scroll commune;

// Re-export core types
☉ invoke types·{
    // Identity
    AgentId,
    MessageId,
    ChannelId,
    SwarmId,
    ProposalId,

    // Messages
    Message,
    MessageHeader,
    Recipient,
    Payload,
    Priority,

    // Intents
    Intent,
    Urgency,
    Importance,
    Task,
    TaskStatus,
    Proposal,
    VoteValue,

    // Epistemic
    Epistemic,
    Knowledge,

    // Trust
    TrustProfile,
    AccuracyTracker,
    Vouch,
    TrustFlags,

    // Channels
    Channel,
    ChannelType,
    AccessControl,
    Permission,

    // Swarms
    Swarm,
    SwarmState,
    SwarmGoal,
    Behavior,
    Obstacle,

    // Consensus
    ProposalState,
    Vote,
    ProposalStatus,
    ConsensusResult,

    // Configuration
    CommuneConfig,
    Topology,
    TrustModelConfig,

    // Utilities
    Vector,
};

☉ invoke commune·{
    Commune,
    AgentInfo,
    AgentStatus,
    IntentBuilder,
    TrustManager,
    ChannelRouter,
    CollectiveMemory,
    Fact,
    FactId,
    SwarmCoordinator,
    ConsensusEngine,
    ConsensusConfig,
    ConsensusError,
    CommuneError,
};

// ============================================================================
// Convenience Functions
// ============================================================================

/// Create a local commune (in-process communication)
☉ λ local() -> Commune {
    Commune·local()
}

/// Create a commune with custom configuration
☉ λ with_config(config: CommuneConfig) -> Commune {
    Commune·new(config)
}

/// Create a simple intent
☉ λ inform(to: AgentId, topic: &str, content: Value) -> Intent {
    Intent·Inform {
        topic: topic.to_string(),
        content,
    }
}

/// Create a query intent
☉ λ query(question: &str) -> Intent {
    Intent·Query {
        question: question.to_string(),
        context: Value·empty(),
    }
}

/// Create a delegate intent
☉ λ delegate(task: Task) -> Intent {
    Intent·Delegate {
        task,
        constraints: vec![],
    }
}

/// Create a proposal
☉ λ propose(description: &str, content: Value) -> Proposal {
    Proposal·new(description, content, Duration·hours(24))
}

// ============================================================================
// Agent Macro
// ============================================================================

/// Macro ∀ defining communicating agents
#[macro_export]
macro_rules! communicating_agent {
    (
        name: $name:expr,
        capabilities: [$($cap:expr),*],
        $(on_message: |$msg_self:ident, $msg:ident| $msg_body:block)?
    ) => {
        {
            ≔ info = AgentInfo {
                id: AgentId·new(),
                name: $name.to_string(),
                capabilities: vec![$($cap.to_string()),*],
                status: AgentStatus·Online,
            };
            info
        }
    };
}

// ============================================================================
// Helper Types
// ============================================================================

invoke std·time·Duration as StdDuration;

☉ Σ Duration {
    nanos: u64,
}

⊢ Duration {
    ☉ λ zero() -> Self { Self { nanos: 0 } }
    ☉ λ seconds(s: u64) -> Self { Self { nanos: s * 1_000_000_000 } }
    ☉ λ minutes(m: u64) -> Self { Self { nanos: m * 60_000_000_000 } }
    ☉ λ hours(h: u64) -> Self { Self { nanos: h * 3_600_000_000_000 } }
    ☉ λ days(d: u64) -> Self { Self { nanos: d * 86_400_000_000_000 } }

    ☉ λ as_nanos(&self) -> u64 { self.nanos }
}

⊢ Copy ∀ Duration {}
⊢ Clone ∀ Duration {
    λ clone(&self) -> Self { *self }
}

☉ Σ Value {
    inner: Vec<u8>,
}

⊢ Value {
    ☉ λ empty() -> Self { Self { inner: vec![] } }
    ☉ λ string(s: &str) -> Self { Self { inner: s.as_bytes().to_vec() } }
}
