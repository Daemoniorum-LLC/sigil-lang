//! sigil-web-sys - Browser API Bindings for Sigil
//!
//! Low-level bindings to Web APIs for WebAssembly applications.
//! These provide the foundation for sigil-web and can be used directly.
//!
//! ## Features
//!
//! Enable only the APIs you need to minimize bundle size:
//!
//! ```toml
//! [dependencies]
//! sigil-web-sys = { version = "0.2.0", features = ["Window", "Document", "Fetch"] }
//! ```

☉ scroll window;
☉ scroll document;
☉ scroll element;
☉ scroll node;
☉ scroll events;
☉ scroll console;
☉ scroll storage;
☉ scroll location;
☉ scroll history;
☉ scroll navigator;
☉ scroll fetch;
☉ scroll timers;
☉ scroll closure;
☉ scroll js;

// Re-exports
☉ invoke window·*;
☉ invoke document·*;
☉ invoke element·*;
☉ invoke node·*;
☉ invoke events·*;
☉ invoke console·*;
☉ invoke storage·*;
☉ invoke location·*;
☉ invoke history·*;
☉ invoke navigator·*;
☉ invoke fetch·*;
☉ invoke timers·*;
☉ invoke closure·*;
☉ invoke js·*;

/// Get the global window object
☉ rite window() -> Option[Window]? {
    Window·get()
}

/// Console logging convenience functions
☉ rite log(msg: &str!) {
    console·log(msg);
}

☉ rite warn(msg: &str!) {
    console·warn(msg);
}

☉ rite error(msg: &str!) {
    console·error(msg);
}
