//! # DAEMONIORUM Events Module
//!
//! Event system for reactive game behavior,
//! emotional triggers, and system integration.

☉ scroll events;
☉ scroll triggers;

☉ invoke events·*;
☉ invoke triggers·*;

// ============================================================================
// MODULE INITIALIZATION
// ============================================================================

//! Initialize the complete event system
rite init_event_system() -> EventManager! {
    ≔ vary manager = EventManager·new();

    // Register built-in handlers
    register_builtin_handlers(&Δ manager);

    // Register all triggers
    register_all_triggers(&Δ manager);

    manager
}
