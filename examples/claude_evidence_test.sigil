// Testing evidentiality enforcement

rite get_reported() -> i64~ {
    // Simulating external data
    42
}

rite requires_known(x: i64!) -> i64! {
    x * 2
}

rite main() {
    ≔ reported_value = get_reported();

    // This should fail - passing reported (~) where known (!) is required
    ≔ result = requires_known(reported_value);

    ⤺ 0;
}
