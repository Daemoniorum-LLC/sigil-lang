rite fib(n) {
    ⎇ n <= 1 {
        ⤺ n;
    }
    ⤺ fib(n - 1) + fib(n - 2);
}

rite main() {
    print("Starting...");
    ≔ start = now();
    ≔ result = fib(25);
    ≔ elapsed = now() - start;
    print("Fib(25) =");
    print(result);
    print("Time (ms):");
    print(elapsed);
    ⤺ 0;
}
