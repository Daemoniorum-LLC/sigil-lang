//! Test file for advanced Sigil syntax
//! Tests @[] attributes and optional semicolons

invoke HashMap

@[derive(Debug, Clone)]
Σ Point {
    x: i32,
    y: i32,
}

@[derive(Default)]
Σ Config {
    name: str,
    value: i32,
}

const PI: f64 = 3.14159

rite main() {
    ≔ x = 10;
    ≔ y = 20;
    ≔ sum = x + y;
    print(sum);
    ⤺ 0;
}
