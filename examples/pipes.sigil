// Pipe operations in Sigil
rite main() → i64 {
    ≔ nums = [1, 2, 3, 4, 5];

    // Transform with tau
    ≔ doubled = nums|τ{_ * 2};
    print(doubled);

    // Sum
    ≔ total = nums|Σ;
    print(total);

    0
}
