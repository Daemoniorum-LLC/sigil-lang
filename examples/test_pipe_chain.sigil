fn main() {
    let nums = vec![1, 2, 3, 4, 5, 6];

    // Chain: filter evens, double them, sum
    let result = nums
        |filter(|x| x % 2 == 0)
        |map(|x| x * 2)
        |fold(0, |acc, x| acc + x);

    println(result);  // (2+4+6) * 2 = 24
}
