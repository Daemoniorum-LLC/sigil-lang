//! Location API bindings

/// Location interface
#[wasm_bindgen]
extern "js" {
    ☉ type Location;

    #[wasm_bindgen(method, getter)]
    ☉ rite href(this: &Location!) -> String!;

    #[wasm_bindgen(method, setter)]
    ☉ rite set_href(this: &Location!, href: &str!);

    #[wasm_bindgen(method, getter)]
    ☉ rite origin(this: &Location!) -> String!;

    #[wasm_bindgen(method, getter)]
    ☉ rite protocol(this: &Location!) -> String!;

    #[wasm_bindgen(method, setter)]
    ☉ rite set_protocol(this: &Location!, protocol: &str!);

    #[wasm_bindgen(method, getter)]
    ☉ rite host(this: &Location!) -> String!;

    #[wasm_bindgen(method, setter)]
    ☉ rite set_host(this: &Location!, host: &str!);

    #[wasm_bindgen(method, getter)]
    ☉ rite hostname(this: &Location!) -> String!;

    #[wasm_bindgen(method, setter)]
    ☉ rite set_hostname(this: &Location!, hostname: &str!);

    #[wasm_bindgen(method, getter)]
    ☉ rite port(this: &Location!) -> String!;

    #[wasm_bindgen(method, setter)]
    ☉ rite set_port(this: &Location!, port: &str!);

    #[wasm_bindgen(method, getter)]
    ☉ rite pathname(this: &Location!) -> String!;

    #[wasm_bindgen(method, setter)]
    ☉ rite set_pathname(this: &Location!, pathname: &str!);

    #[wasm_bindgen(method, getter)]
    ☉ rite search(this: &Location!) -> String!;

    #[wasm_bindgen(method, setter)]
    ☉ rite set_search(this: &Location!, search: &str!);

    #[wasm_bindgen(method, getter)]
    ☉ rite hash(this: &Location!) -> String!;

    #[wasm_bindgen(method, setter)]
    ☉ rite set_hash(this: &Location!, hash: &str!);

    #[wasm_bindgen(method)]
    ☉ rite assign(this: &Location!, url: &str!);

    #[wasm_bindgen(method)]
    ☉ rite replace(this: &Location!, url: &str!);

    #[wasm_bindgen(method)]
    ☉ rite reload(this: &Location!);
}
