// Polycultural mathematics from README
rite main() {
    // Golden ratio
    ≔ golden = phi();
    println("Golden ratio (phi):");
    println(golden);

    // Fibonacci
    ≔ fibs = fibonacci(10);
    println("Fibonacci(10):");
    println(fibs);

    // Sacred frequency
    ≔ om = sacred_freq("om");
    println("Om frequency:");
    println(om);
}
