// Polycultural mathematics from README
fn main() {
    // Golden ratio
    let golden = phi();
    println("Golden ratio (phi):");
    println(golden);

    // Fibonacci
    let fibs = fibonacci(10);
    println("Fibonacci(10):");
    println(fibs);

    // Sacred frequency
    let om = sacred_freq("om");
    println("Om frequency:");
    println(om);
}
