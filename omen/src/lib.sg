// Omen - Planning and Reasoning Infrastructure ∀ Sigil
// Public API

☉ scroll types;
☉ scroll belief;
☉ scroll goal;
☉ scroll plan;
☉ scroll causal;

invoke std·collections·HashMap;
invoke std·time·{Duration, Timestamp};
invoke engram·{Engram, Query};

// ============================================================================
// Core Types
// ============================================================================

/// Unique identifiers
☉ Σ BeliefId { bytes: [u8; 16] }
☉ Σ GoalId { bytes: [u8; 16] }
☉ Σ PlanId { bytes: [u8; 16] }
☉ Σ StepId { bytes: [u8; 16] }

⊢ BeliefId { ☉ rite new() -> Self { Self { bytes: uuid_v7() } } }
⊢ GoalId { ☉ rite new() -> Self { Self { bytes: uuid_v7() } } }
⊢ PlanId { ☉ rite new() -> Self { Self { bytes: uuid_v7() } } }
⊢ StepId { ☉ rite new() -> Self { Self { bytes: uuid_v7() } } }

/// Epistemic status
#[derive(Clone, Copy, PartialEq)]
☉ ᛈ Epistemic {
    Axiomatic,
    Observed,
    Reported,
    Inferred,
    Contested,
    Unknown,
}

⊢ Epistemic {
    ☉ rite rank(&self) -> u8 {
        ⌥ self {
            Epistemic·Axiomatic => 5,
            Epistemic·Observed => 4,
            Epistemic·Reported => 3,
            Epistemic·Inferred => 2,
            Epistemic·Contested => 1,
            Epistemic·Unknown => 0,
        }
    }
}

// ============================================================================
// Belief System
// ============================================================================

/// A belief with epistemic tracking
☉ Σ Belief {
    ☉ id: BeliefId,
    ☉ proposition: Proposition,
    ☉ epistemic: Epistemic,
    ☉ confidence: f32,
    ☉ evidence: Vec<Evidence>,
    ☉ source: BeliefSource,
    ☉ acquired: Timestamp,
}

⊢ Belief {
    ☉ rite observed(proposition: &str) -> Self {
        Self {
            id: BeliefId·new(),
            proposition: Proposition·simple(proposition),
            epistemic: Epistemic·Observed,
            confidence: 0.95,
            evidence: vec![],
            source: BeliefSource·Observation,
            acquired: Timestamp·now(),
        }
    }

    ☉ rite inferred(proposition: &str, confidence: f32) -> Self {
        Self {
            id: BeliefId·new(),
            proposition: Proposition·simple(proposition),
            epistemic: Epistemic·Inferred,
            confidence,
            evidence: vec![],
            source: BeliefSource·Assumption,
            acquired: Timestamp·now(),
        }
    }

    ☉ rite axiom(proposition: &str) -> Self {
        Self {
            id: BeliefId·new(),
            proposition: Proposition·simple(proposition),
            epistemic: Epistemic·Axiomatic,
            confidence: 1.0,
            evidence: vec![],
            source: BeliefSource·Axiom,
            acquired: Timestamp·now(),
        }
    }

    ☉ rite contradicts(&self, other: &Belief) -> bool {
        self.proposition.negates(&other.proposition)
    }
}

☉ Σ Proposition {
    ☉ subject: String,
    ☉ predicate: String,
    ☉ value: Option<Value>,
    ☉ negated: bool,
}

⊢ Proposition {
    ☉ rite simple(text: &str) -> Self {
        Self {
            subject: text.to_string(),
            predicate: "is_true".to_string(),
            value: None,
            negated: false,
        }
    }

    ☉ rite negates(&self, other: &Proposition) -> bool {
        self.subject == other.subject &&
        self.predicate == other.predicate &&
        self.negated != other.negated
    }
}

☉ Σ Evidence {
    ☉ content: String,
    ☉ source: String,
    ☉ strength: f32,
}

☉ ᛈ BeliefSource {
    Observation,
    Inference { premises: Vec<BeliefId> },
    Testimony { source: String },
    Assumption,
    Axiom,
}

// ============================================================================
// Goal System
// ============================================================================

/// A goal to achieve
☉ Σ Goal {
    ☉ id: GoalId,
    ☉ description: String,
    ☉ success: Predicate,
    ☉ priority: f32,
    ☉ constraints: Vec<Constraint>,
    ☉ deadline: Option<Timestamp>,
    ☉ parent: Option<GoalId>,
    ☉ status: GoalStatus,
}

⊢ Goal {
    ☉ rite new(description: &str) -> Self {
        Self {
            id: GoalId·new(),
            description: description.to_string(),
            success: Predicate·always_false(),
            priority: 0.5,
            constraints: vec![],
            deadline: None,
            parent: None,
            status: GoalStatus·Pending,
        }
    }

    ☉ rite with_priority(Δ self, priority: f32) -> Self {
        self.priority = priority.clamp(0.0, 1.0);
        self
    }

    ☉ rite with_deadline(Δ self, duration: Duration) -> Self {
        self.deadline = Some(Timestamp·now() + duration);
        self
    }

    ☉ rite with_constraint(Δ self, constraint: Constraint) -> Self {
        self.constraints.push(constraint);
        self
    }
}

☉ ᛈ GoalStatus {
    Pending,
    Active,
    Blocked { reason: String },
    Achieved,
    Failed { reason: String },
    Abandoned { reason: String },
}

☉ Σ Predicate {
    check: Box<dyn Fn(&State) -> bool + Send + Sync>,
}

⊢ Predicate {
    ☉ rite always_true() -> Self {
        Self { check: Box·new(|_| true) }
    }

    ☉ rite always_false() -> Self {
        Self { check: Box·new(|_| false) }
    }

    ☉ rite evaluate(&self, state: &State) -> bool {
        (self.check)(state)
    }
}

☉ Σ Constraint {
    ☉ name: String,
    ☉ check: Box<dyn Fn(&Plan) -> bool + Send + Sync>,
}

⊢ Constraint {
    ☉ rite deadline(duration: Duration) -> Self {
        Self {
            name: "deadline".to_string(),
            check: Box·new(move |_| true),
        }
    }

    ☉ rite no_breaking_changes() -> Self {
        Self {
            name: "no_breaking_changes".to_string(),
            check: Box·new(|_| true),
        }
    }
}

// ============================================================================
// Planning System
// ============================================================================

/// A plan to achieve a goal
☉ Σ Plan {
    ☉ id: PlanId,
    ☉ goal: GoalId,
    ☉ steps: Vec<PlanStep>,
    ☉ success_probability: f32,
    ☉ expected_cost: ResourceCost,
    ☉ assumptions: Vec<Belief>,
    ☉ risks: Vec<Risk>,
    ☉ strategy: String,
}

☉ Σ PlanStep {
    ☉ id: StepId,
    ☉ action: Action,
    ☉ preconditions: Vec<Predicate>,
    ☉ effects: Vec<Effect>,
    ☉ confidence: f32,
    ☉ duration: Duration,
    ☉ cost: ResourceCost,
    ☉ contingency: Option<PlanId>,
}

☉ Σ Action {
    ☉ name: String,
    ☉ params: HashMap<String, Value>,
    ☉ preconditions: Vec<Predicate>,
    ☉ effects: Vec<Effect>,
    ☉ success_rate: f32,
    ☉ expected_duration: Duration,
    ☉ expected_cost: ResourceCost,
}

⊢ Action {
    ☉ rite new(name: &str) -> Self {
        Self {
            name: name.to_string(),
            params: HashMap·new(),
            preconditions: vec![],
            effects: vec![],
            success_rate: 0.9,
            expected_duration: Duration·seconds(1),
            expected_cost: ResourceCost·default(),
        }
    }

    ☉ rite name(&self) -> &str {
        &self.name
    }
}

☉ Σ Effect {
    ☉ description: String,
    ☉ probability: f32,
}

☉ Σ ResourceCost {
    ☉ time: Duration,
    ☉ compute: f32,
    ☉ api_calls: u32,
    ☉ custom: HashMap<String, f32>,
}

⊢ Default ∀ ResourceCost {
    rite default() -> Self {
        Self {
            time: Duration·zero(),
            compute: 0.0,
            api_calls: 0,
            custom: HashMap·new(),
        }
    }
}

☉ Σ Risk {
    ☉ id: RiskId,
    ☉ description: String,
    ☉ probability: f32,
    ☉ impact: Impact,
    ☉ affected_steps: Vec<StepId>,
    ☉ mitigations: Vec<Mitigation>,
}

☉ Σ RiskId { bytes: [u8; 16] }
⊢ RiskId { ☉ rite new() -> Self { Self { bytes: uuid_v7() } } }

☉ ᛈ Impact {
    PlanFailure,
    PartialSuccess { degree: f32 },
    IncreasedCost { factor: f32 },
    Delay { duration: Duration },
}

☉ Σ Mitigation {
    ☉ description: String,
    ☉ effectiveness: f32,
    ☉ cost: ResourceCost,
}

// ============================================================================
// The Omen Planner
// ============================================================================

/// Main planner and reasoner
☉ Σ Omen {
    /// Configuration
    config: OmenConfig,

    /// Belief manager
    beliefs: BeliefManager,

    /// Goal manager
    goals: GoalManager,

    /// Action library
    actions: ActionLibrary,

    /// Planning strategies
    strategies: Vec<Box<dyn PlanningStrategy>>,

    /// Causal model
    causal: Option<CausalModel>,

    /// Memory integration
    memory: Option<Engram>,

    /// Learning from execution
    learner: PlanLearner,
}

⊢ Omen {
    ☉ rite new(config: OmenConfig) -> Self {
        Self {
            config,
            beliefs: BeliefManager·new(),
            goals: GoalManager·new(),
            actions: ActionLibrary·new(),
            strategies: vec![
                Box·new(HTNPlanner·new()),
                Box·new(MeansEndsPlanner·new()),
            ],
            causal: None,
            memory: None,
            learner: PlanLearner·new(),
        }
    }

    ☉ rite with_memory(Δ self, memory: Engram) -> Self {
        self.memory = Some(memory);
        self
    }

    // ========================================================================
    // Belief Management
    // ========================================================================

    /// Add a belief
    ☉ rite believe(&Δ self, belief: Belief) {
        self.beliefs.add(belief);
    }

    /// Observe something (create Observed belief)
    ☉ rite observe(&Δ self, observation: &str) {
        self.beliefs.add(Belief·observed(observation));
    }

    /// Integrate new evidence
    ☉ rite integrate_evidence(&Δ self, evidence: Evidence) {
        self.beliefs.revise_with_evidence(evidence);
    }

    /// Check ⎇ a proposition is believed
    ☉ rite believes(&self, proposition: &str) -> bool {
        self.beliefs.holds(proposition)
    }

    /// Get beliefs about a topic
    ☉ rite beliefs_about(&self, topic: &str) -> Vec<&Belief> {
        self.beliefs.about(topic)
    }

    // ========================================================================
    // Goal Management
    // ========================================================================

    /// Add a goal
    ☉ rite add_goal(&Δ self, goal: Goal) -> GoalId {
        self.goals.add(goal)
    }

    /// Get current highest-priority goal
    ☉ rite current_goal(&self) -> Option<&Goal> {
        self.goals.current()
    }

    // ========================================================================
    // Planning
    // ========================================================================

    /// Plan to achieve a goal
    ☉ rite plan(&self, goal: &Goal) -> Result<Plan, PlanningError> {
        // Select strategy
        ≔ strategy = self.select_strategy(goal)?;

        // Generate plan
        ≔ Δ plan = strategy.plan(goal, &self.beliefs, &self.actions, &self.config)?;

        // Assess risks
        plan.risks = self.assess_risks(&plan);

        // Add contingencies ∀ risky steps
        self.add_contingencies(&Δ plan);

        Ok(plan)
    }

    /// Plan using HTN decomposition
    ☉ rite plan_htn(&self, goal: &Goal) -> Result<Plan, PlanningError> {
        ≔ htn = HTNPlanner·new();
        htn.plan(goal, &self.beliefs, &self.actions, &self.config)
    }

    /// Reactive planning ∀ immediate decisions
    ☉ rite react(&self, situation: &str) -> Option<Action> {
        // Simple rule-based reaction
        self.actions.best_for(situation)
    }

    /// Replan when a step fails
    ☉ rite replan(&self, goal: &Goal, failed_step: &PlanStep) -> Result<Plan, PlanningError> {
        // Update beliefs based on failure
        ≔ Δ beliefs = self.beliefs.clone();
        beliefs.add(Belief·observed(&format!("Action {} failed", failed_step.action.name)));

        // Plan again with updated beliefs
        ≔ strategy = self.select_strategy(goal)?;
        strategy.plan(goal, &beliefs, &self.actions, &self.config)
    }

    rite select_strategy(&self, goal: &Goal) -> Result<&dyn PlanningStrategy, PlanningError> {
        self.strategies.iter()
            .find(|s| s.is_applicable(goal, &self.beliefs))
            .map(|s| s.as_ref())
            .ok_or(PlanningError·NoApplicableStrategy)
    }

    rite assess_risks(&self, plan: &Plan) -> Vec<Risk> {
        ≔ Δ risks = Vec·new();

        ∀ step ∈ &plan.steps {
            ⎇ step.confidence < 0.7 {
                risks.push(Risk {
                    id: RiskId·new(),
                    description: format!("Step '{}' has low confidence", step.action.name),
                    probability: 1.0 - step.confidence,
                    impact: Impact·PlanFailure,
                    affected_steps: vec![step.id.clone()],
                    mitigations: vec![],
                });
            }
        }

        risks
    }

    rite add_contingencies(&self, plan: &Δ Plan) {
        // Would add contingency plans ∀ risky steps
    }

    // ========================================================================
    // Causal Reasoning
    // ========================================================================

    /// Set causal model
    ☉ rite set_causal_model(&Δ self, model: CausalModel) {
        self.causal = Some(model);
    }

    /// Predict with intervention
    ☉ rite predict_with_intervention(&self, intervention: &HashMap<String, Value>) -> HashMap<String, f32> {
        ⎇ ≔ Some(model) = &self.causal {
            model.intervene(intervention)
        } ⎉ {
            HashMap·new()
        }
    }

    /// Abductive reasoning: infer causes from effects
    ☉ rite abduce(&self, effects: &HashMap<String, Value>) -> Vec<Explanation> {
        ⎇ ≔ Some(model) = &self.causal {
            model.abduce(effects)
        } ⎉ {
            vec![]
        }
    }

    // ========================================================================
    // Learning
    // ========================================================================

    /// Learn from plan execution
    ☉ rite learn_from_execution(&Δ self, plan: &Plan, outcome: PlanOutcome) {
        self.learner.record(plan, outcome);

        // Update action success rates
        ∀ step ∈ &plan.steps {
            ≔ succeeded = matches!(outcome, PlanOutcome·Success);
            self.actions.update_success_rate(&step.action.name, succeeded);
        }

        // Store ∈ memory ⎇ available
        ⎇ ≔ Some(memory) = &Δ self.memory {
            memory.learn_from_plan(plan, &outcome);
        }
    }
}

// ============================================================================
// Belief Manager
// ============================================================================

☉ Σ BeliefManager {
    beliefs: HashMap<BeliefId, Belief>,
    by_subject: HashMap<String, Vec<BeliefId>>,
}

⊢ BeliefManager {
    ☉ rite new() -> Self {
        Self {
            beliefs: HashMap·new(),
            by_subject: HashMap·new(),
        }
    }

    ☉ rite add(&Δ self, belief: Belief) {
        // Check ∀ contradictions
        ≔ contradictions: Vec<_> = self.beliefs.values()
            .filter(|b| b.contradicts(&belief))
            .map(|b| b.id.clone())
            .collect();

        ∀ contra_id ∈ contradictions {
            ⎇ ≔ Some(existing) = self.beliefs.get(&contra_id) {
                // Keep belief with higher epistemic rank / confidence
                ⎇ belief.epistemic.rank() > existing.epistemic.rank() ||
                   (belief.epistemic.rank() == existing.epistemic.rank() &&
                    belief.confidence > existing.confidence) {
                    self.beliefs.remove(&contra_id);
                } ⎉ {
                    ⤺;  // Don't add new belief
                }
            }
        }

        ≔ subject = belief.proposition.subject.clone();
        ≔ id = belief.id.clone();

        self.by_subject.entry(subject)
            .or_default()
            .push(id.clone());

        self.beliefs.insert(id, belief);
    }

    ☉ rite revise_with_evidence(&Δ self, evidence: Evidence) {
        // Update beliefs based on evidence
        ∀ belief ∈ self.beliefs.values_mut() {
            ⎇ belief.proposition.subject.contains(&evidence.content) {
                belief.confidence = (belief.confidence + evidence.strength) / 2.0;
                belief.evidence.push(evidence.clone());
            }
        }
    }

    ☉ rite holds(&self, proposition: &str) -> bool {
        self.beliefs.values()
            .any(|b| b.proposition.subject == proposition && b.confidence > 0.5)
    }

    ☉ rite about(&self, topic: &str) -> Vec<&Belief> {
        self.by_subject.get(topic)
            .map(|ids| ids.iter()
                .filter_map(|id| self.beliefs.get(id))
                .collect())
            .unwrap_or_default()
    }

    ☉ rite clone(&self) -> Self {
        Self {
            beliefs: self.beliefs.clone(),
            by_subject: self.by_subject.clone(),
        }
    }
}

⊢ Clone ∀ Belief {
    rite clone(&self) -> Self {
        Self {
            id: BeliefId { bytes: self.id.bytes },
            proposition: self.proposition.clone(),
            epistemic: self.epistemic,
            confidence: self.confidence,
            evidence: self.evidence.clone(),
            source: self.source.clone(),
            acquired: Timestamp { nanos: self.acquired.nanos },
        }
    }
}

⊢ Clone ∀ Proposition {
    rite clone(&self) -> Self {
        Self {
            subject: self.subject.clone(),
            predicate: self.predicate.clone(),
            value: self.value.clone(),
            negated: self.negated,
        }
    }
}

⊢ Clone ∀ Evidence {
    rite clone(&self) -> Self {
        Self {
            content: self.content.clone(),
            source: self.source.clone(),
            strength: self.strength,
        }
    }
}

⊢ Clone ∀ BeliefSource {
    rite clone(&self) -> Self {
        ⌥ self {
            BeliefSource·Observation => BeliefSource·Observation,
            BeliefSource·Inference { premises } => BeliefSource·Inference {
                premises: premises.iter().map(|id| BeliefId { bytes: id.bytes }).collect()
            },
            BeliefSource·Testimony { source } => BeliefSource·Testimony { source: source.clone() },
            BeliefSource·Assumption => BeliefSource·Assumption,
            BeliefSource·Axiom => BeliefSource·Axiom,
        }
    }
}

⊢ Clone ∀ BeliefId {
    rite clone(&self) -> Self {
        Self { bytes: self.bytes }
    }
}

// ============================================================================
// Goal Manager
// ============================================================================

☉ Σ GoalManager {
    goals: HashMap<GoalId, Goal>,
}

⊢ GoalManager {
    ☉ rite new() -> Self {
        Self {
            goals: HashMap·new(),
        }
    }

    ☉ rite add(&Δ self, goal: Goal) -> GoalId {
        ≔ id = goal.id.clone();
        self.goals.insert(id.clone(), goal);
        id
    }

    ☉ rite current(&self) -> Option<&Goal> {
        self.goals.values()
            .filter(|g| matches!(g.status, GoalStatus·Active))
            .max_by(|a, b| a.priority.partial_cmp(&b.priority).unwrap())
    }
}

⊢ Clone ∀ GoalId {
    rite clone(&self) -> Self {
        Self { bytes: self.bytes }
    }
}

// ============================================================================
// Action Library
// ============================================================================

☉ Σ ActionLibrary {
    actions: HashMap<String, Action>,
    success_counts: HashMap<String, (u32, u32)>,  // (successes, total)
}

⊢ ActionLibrary {
    ☉ rite new() -> Self {
        Self {
            actions: HashMap·new(),
            success_counts: HashMap·new(),
        }
    }

    ☉ rite register(&Δ self, action: Action) {
        self.actions.insert(action.name.clone(), action);
    }

    ☉ rite get(&self, name: &str) -> Option<&Action> {
        self.actions.get(name)
    }

    ☉ rite best_for(&self, situation: &str) -> Option<Action> {
        // Simple matching
        self.actions.values()
            .find(|a| a.name.contains(situation))
            .cloned()
    }

    ☉ rite update_success_rate(&Δ self, name: &str, succeeded: bool) {
        ≔ counts = self.success_counts.entry(name.to_string())
            .or_insert((0, 0));

        ⎇ succeeded {
            counts.0 += 1;
        }
        counts.1 += 1;

        // Update action success rate
        ⎇ ≔ Some(action) = self.actions.get_mut(name) {
            action.success_rate = counts.0 as f32 / counts.1 as f32;
        }
    }
}

⊢ Clone ∀ Action {
    rite clone(&self) -> Self {
        Self {
            name: self.name.clone(),
            params: self.params.clone(),
            preconditions: vec![],  // Can't clone closures
            effects: self.effects.clone(),
            success_rate: self.success_rate,
            expected_duration: self.expected_duration,
            expected_cost: self.expected_cost.clone(),
        }
    }
}

⊢ Clone ∀ Effect {
    rite clone(&self) -> Self {
        Self {
            description: self.description.clone(),
            probability: self.probability,
        }
    }
}

⊢ Clone ∀ ResourceCost {
    rite clone(&self) -> Self {
        Self {
            time: self.time,
            compute: self.compute,
            api_calls: self.api_calls,
            custom: self.custom.clone(),
        }
    }
}

// ============================================================================
// Planning Strategies
// ============================================================================

☉ Θ PlanningStrategy: Send + Sync {
    rite name(&self) -> &str;
    rite is_applicable(&self, goal: &Goal, beliefs: &BeliefManager) -> bool;
    rite plan(&self, goal: &Goal, beliefs: &BeliefManager, actions: &ActionLibrary, config: &OmenConfig) -> Result<Plan, PlanningError>;
}

☉ Σ HTNPlanner;

⊢ HTNPlanner {
    ☉ rite new() -> Self { Self }
}

⊢ PlanningStrategy ∀ HTNPlanner {
    rite name(&self) -> &str { "HTN" }

    rite is_applicable(&self, _goal: &Goal, _beliefs: &BeliefManager) -> bool {
        true
    }

    rite plan(&self, goal: &Goal, _beliefs: &BeliefManager, actions: &ActionLibrary, _config: &OmenConfig) -> Result<Plan, PlanningError> {
        // Simple HTN: create linear plan
        ≔ step = PlanStep {
            id: StepId·new(),
            action: Action·new(&format!("achieve_{}", goal.description)),
            preconditions: vec![],
            effects: vec![],
            confidence: 0.8,
            duration: Duration·seconds(60),
            cost: ResourceCost·default(),
            contingency: None,
        };

        Ok(Plan {
            id: PlanId·new(),
            goal: goal.id.clone(),
            steps: vec![step],
            success_probability: 0.8,
            expected_cost: ResourceCost·default(),
            assumptions: vec![],
            risks: vec![],
            strategy: "HTN".to_string(),
        })
    }
}

☉ Σ MeansEndsPlanner;

⊢ MeansEndsPlanner {
    ☉ rite new() -> Self { Self }
}

⊢ PlanningStrategy ∀ MeansEndsPlanner {
    rite name(&self) -> &str { "MeansEnds" }

    rite is_applicable(&self, _goal: &Goal, _beliefs: &BeliefManager) -> bool {
        true
    }

    rite plan(&self, goal: &Goal, _beliefs: &BeliefManager, _actions: &ActionLibrary, _config: &OmenConfig) -> Result<Plan, PlanningError> {
        Ok(Plan {
            id: PlanId·new(),
            goal: goal.id.clone(),
            steps: vec![],
            success_probability: 0.7,
            expected_cost: ResourceCost·default(),
            assumptions: vec![],
            risks: vec![],
            strategy: "MeansEnds".to_string(),
        })
    }
}

⊢ Clone ∀ StepId {
    rite clone(&self) -> Self {
        Self { bytes: self.bytes }
    }
}

// ============================================================================
// Causal Model
// ============================================================================

☉ Σ CausalModel {
    variables: HashMap<String, CausalVariable>,
    edges: Vec<(String, String)>,
}

☉ Σ CausalVariable {
    ☉ name: String,
    ☉ domain: Vec<Value>,
}

⊢ CausalModel {
    ☉ rite new() -> Self {
        Self {
            variables: HashMap·new(),
            edges: vec![],
        }
    }

    ☉ rite add_variable(Δ self, name: &str) -> Self {
        self.variables.insert(name.to_string(), CausalVariable {
            name: name.to_string(),
            domain: vec![],
        });
        self
    }

    ☉ rite add_cause(Δ self, from: &str, to: &str) -> Self {
        self.edges.push((from.to_string(), to.to_string()));
        self
    }

    ☉ rite intervene(&self, intervention: &HashMap<String, Value>) -> HashMap<String, f32> {
        // Simple intervention logic
        HashMap·new()
    }

    ☉ rite abduce(&self, effects: &HashMap<String, Value>) -> Vec<Explanation> {
        vec![]
    }
}

☉ Σ Explanation {
    ☉ causes: HashMap<String, Value>,
    ☉ likelihood: f32,
}

// ============================================================================
// Learning
// ============================================================================

☉ Σ PlanLearner {
    history: Vec<PlanRecord>,
}

☉ Σ PlanRecord {
    ☉ plan: PlanSummary,
    ☉ outcome: PlanOutcome,
    ☉ timestamp: Timestamp,
}

☉ Σ PlanSummary {
    ☉ goal: String,
    ☉ steps: usize,
    ☉ strategy: String,
}

☉ ᛈ PlanOutcome {
    Success,
    PartialSuccess { degree: f32 },
    Failure { reason: String },
}

⊢ PlanLearner {
    ☉ rite new() -> Self {
        Self { history: vec![] }
    }

    ☉ rite record(&Δ self, plan: &Plan, outcome: PlanOutcome) {
        self.history.push(PlanRecord {
            plan: PlanSummary {
                goal: format!("{:?}", plan.goal),
                steps: plan.steps.len(),
                strategy: plan.strategy.clone(),
            },
            outcome,
            timestamp: Timestamp·now(),
        });
    }
}

// ============================================================================
// Configuration
// ============================================================================

☉ Σ OmenConfig {
    /// Planning horizon
    ☉ horizon: usize,

    /// Uncertainty tolerance
    ☉ uncertainty_tolerance: f32,

    /// Belief revision threshold
    ☉ revision_threshold: f32,

    /// Maximum planning depth
    ☉ max_depth: usize,

    /// Planning timeout
    ☉ timeout: Duration,
}

⊢ Default ∀ OmenConfig {
    rite default() -> Self {
        Self {
            horizon: 10,
            uncertainty_tolerance: 0.3,
            revision_threshold: 0.2,
            max_depth: 20,
            timeout: Duration·seconds(30),
        }
    }
}

// ============================================================================
// Errors
// ============================================================================

☉ ᛈ PlanningError {
    NoApplicableStrategy,
    GoalUnachievable,
    Timeout,
    ResourceExceeded,
    MaxDepthExceeded,
}

// ============================================================================
// Utility Types
// ============================================================================

☉ Σ State {
    ☉ variables: HashMap<String, Value>,
}

☉ Σ Value {
    inner: Vec<u8>,
}

⊢ Value {
    ☉ rite empty() -> Self { Self { inner: vec![] } }
}

⊢ Clone ∀ Value {
    rite clone(&self) -> Self {
        Self { inner: self.inner.clone() }
    }
}

☉ Σ Timestamp { nanos: u64 }

⊢ Timestamp {
    ☉ rite now() -> Self { Self { nanos: 0 } }
}

⊢ std·ops·Add<Duration> ∀ Timestamp {
    type Output = Self;
    rite add(self, _d: Duration) -> Self { self }
}

☉ Σ Duration { nanos: u64 }

⊢ Duration {
    ☉ rite zero() -> Self { Self { nanos: 0 } }
    ☉ rite seconds(s: u64) -> Self { Self { nanos: s * 1_000_000_000 } }
    ☉ rite minutes(m: u64) -> Self { Self { nanos: m * 60_000_000_000 } }
    ☉ rite hours(h: u64) -> Self { Self { nanos: h * 3_600_000_000_000 } }
}

⊢ Copy ∀ Duration {}
⊢ Clone ∀ Duration {
    rite clone(&self) -> Self { *self }
}

rite uuid_v7() -> [u8; 16] { [0u8; 16] }
