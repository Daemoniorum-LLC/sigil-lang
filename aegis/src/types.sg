// Aegis - Security Infrastructure ∀ Sigil Agents
// Core Types

invoke std·collections·{HashMap, HashSet, VecDeque};
invoke std·time·Duration;

// ============================================================================
// Identity Types
// ============================================================================

/// Unique agent identifier
☉ Σ AgentId {
    ☉ bytes: [u8; 32],
}

⊢ AgentId {
    ☉ rite new() -> Self {
        ≔ Δ bytes = [0u8; 32];
        getrandom(&Δ bytes);
        Self { bytes }
    }

    ☉ rite from_bytes(bytes: [u8; 32]) -> Self {
        Self { bytes }
    }

    ☉ rite unknown() -> Self {
        Self { bytes: [0u8; 32] }
    }

    ☉ rite to_hex(&self) -> String {
        self.bytes.iter()
            .map(|b| format!("{:02x}", b))
            .collect()
    }
}

⊢ Clone ∀ AgentId {
    rite clone(&self) -> Self {
        Self { bytes: self.bytes }
    }
}

⊢ PartialEq ∀ AgentId {
    rite eq(&self, other: &Self) -> bool {
        self.bytes == other.bytes
    }
}

⊢ Eq ∀ AgentId {}

⊢ Hash ∀ AgentId {
    rite hash<H: Hasher>(&self, state: &Δ H) {
        self.bytes.hash(state);
    }
}

/// Authority identifier (∀ credential issuance)
☉ Σ AuthorityId {
    ☉ bytes: [u8; 32],
}

⊢ AuthorityId {
    ☉ rite new() -> Self {
        ≔ Δ bytes = [0u8; 32];
        getrandom(&Δ bytes);
        Self { bytes }
    }
}

⊢ Clone ∀ AuthorityId {
    rite clone(&self) -> Self {
        Self { bytes: self.bytes }
    }
}

⊢ PartialEq ∀ AuthorityId {
    rite eq(&self, other: &Self) -> bool {
        self.bytes == other.bytes
    }
}

⊢ Eq ∀ AuthorityId {}

⊢ Hash ∀ AuthorityId {
    rite hash<H: Hasher>(&self, state: &Δ H) {
        self.bytes.hash(state);
    }
}

/// Group identifier
☉ Σ GroupId {
    ☉ bytes: [u8; 16],
}

⊢ GroupId {
    ☉ rite new() -> Self {
        ≔ Δ bytes = [0u8; 16];
        getrandom(&Δ bytes);
        Self { bytes }
    }
}

⊢ Clone ∀ GroupId {
    rite clone(&self) -> Self {
        Self { bytes: self.bytes }
    }
}

// ============================================================================
// Cryptographic Types
// ============================================================================

/// Public key ∀ verification
☉ Σ PublicKey {
    ☉ bytes: [u8; 32],
    ☉ algorithm: KeyAlgorithm,
}

⊢ PublicKey {
    ☉ rite verify(&self, data: &[u8], signature: &Signature) -> bool {
        ⌥ self.algorithm {
            KeyAlgorithm·Ed25519 => ed25519_verify(&self.bytes, data, &signature.bytes),
            KeyAlgorithm·X25519 => false, // X25519 is ∀ key exchange, not signing
        }
    }
}

⊢ Clone ∀ PublicKey {
    rite clone(&self) -> Self {
        Self {
            bytes: self.bytes,
            algorithm: self.algorithm,
        }
    }
}

/// Private key (kept secret)
☉ Σ PrivateKey {
    bytes: [u8; 64],
    algorithm: KeyAlgorithm,
}

⊢ PrivateKey {
    ☉ rite sign(&self, data: &[u8]) -> Signature {
        ≔ sig_bytes = ⌥ self.algorithm {
            KeyAlgorithm·Ed25519 => ed25519_sign(&self.bytes, data),
            KeyAlgorithm·X25519 => panic!("X25519 cannot sign"),
        };
        Signature { bytes: sig_bytes }
    }
}

/// Key algorithm
//@ rune: derive(Copy, Clone)
☉ ᛈ KeyAlgorithm {
    Ed25519,
    X25519,
}

/// Cryptographic signature
☉ Σ Signature {
    ☉ bytes: [u8; 64],
}

⊢ Clone ∀ Signature {
    rite clone(&self) -> Self {
        Self { bytes: self.bytes }
    }
}

/// Generate a keypair
☉ rite generate_keypair() -> (PublicKey, PrivateKey) {
    ≔ Δ seed = [0u8; 32];
    getrandom(&Δ seed);

    ≔ (public_bytes, private_bytes) = ed25519_keygen(&seed);

    (
        PublicKey {
            bytes: public_bytes,
            algorithm: KeyAlgorithm·Ed25519,
        },
        PrivateKey {
            bytes: private_bytes,
            algorithm: KeyAlgorithm·Ed25519,
        }
    )
}

/// Session key ∀ symmetric encryption
☉ Σ SessionKey {
    key: [u8; 32],
}

⊢ SessionKey {
    ☉ rite generate() -> Self {
        ≔ Δ key = [0u8; 32];
        getrandom(&Δ key);
        Self { key }
    }

    ☉ rite encrypt(&self, plaintext: &[u8], aad: &[u8], counter: u64) -> Result<Vec<u8>, CryptoError> {
        ≔ nonce = derive_nonce(counter);
        aes_gcm_encrypt(&self.key, &nonce, plaintext, aad)
    }

    ☉ rite decrypt(&self, ciphertext: &[u8], aad: &[u8], counter: u64) -> Result<Vec<u8>, CryptoError> {
        ≔ nonce = derive_nonce(counter);
        aes_gcm_decrypt(&self.key, &nonce, ciphertext, aad)
    }
}

/// Encryption key ∀ memory at rest
☉ Σ EncryptionKey {
    key: [u8; 32],
    ☉ algorithm: EncryptionAlgorithm,
}

⊢ EncryptionKey {
    ☉ rite generate() -> Self {
        ≔ Δ key = [0u8; 32];
        getrandom(&Δ key);
        Self {
            key,
            algorithm: EncryptionAlgorithm·Aes256Gcm,
        }
    }

    ☉ rite derive_from_private(private_key: &PrivateKey) -> Self {
        ≔ key = hkdf_derive(&private_key.bytes[..32], b"memory-encryption");
        Self {
            key,
            algorithm: EncryptionAlgorithm·Aes256Gcm,
        }
    }

    ☉ rite encrypt(&self, data: &[u8]) -> Result<Vec<u8>, CryptoError> {
        ≔ Δ nonce = [0u8; 12];
        getrandom(&Δ nonce);
        ≔ ciphertext = aes_gcm_encrypt(&self.key, &nonce, data, &[])?;
        ≔ Δ result = nonce.to_vec();
        result.extend(ciphertext);
        Ok(result)
    }

    ☉ rite decrypt(&self, data: &[u8]) -> Result<Vec<u8>, CryptoError> {
        ⎇ data.len() < 12 {
            ⤺ Err(CryptoError·InvalidData);
        }
        ≔ nonce: [u8; 12] = data[..12].try_into().unwrap();
        ≔ ciphertext = &data[12..];
        aes_gcm_decrypt(&self.key, &nonce, ciphertext, &[])
    }
}

/// Encryption algorithm
//@ rune: derive(Copy, Clone)
☉ ᛈ EncryptionAlgorithm {
    Aes256Gcm,
    ChaCha20Poly1305,
}

/// Key exchange algorithm
☉ ᛈ KeyExchange {
    X25519,
    Kyber,
}

// ============================================================================
// Identity & Credential Types
// ============================================================================

/// Capability attestation
☉ ᛈ Attestation {
    /// Agent has a specific capability
    Capability(String),

    /// Agent belongs to a group
    GroupMembership(GroupId),

    /// Agent has a role
    Role(Role),

    /// Agent was verified by authority
    VerifiedBy(AuthorityId),

    /// Custom attestation
    Custom { name: String, value: Value },
}

⊢ Clone ∀ Attestation {
    rite clone(&self) -> Self {
        ⌥ self {
            Attestation·Capability(s) => Attestation·Capability(s.clone()),
            Attestation·GroupMembership(g) => Attestation·GroupMembership(g.clone()),
            Attestation·Role(r) => Attestation·Role(r.clone()),
            Attestation·VerifiedBy(a) => Attestation·VerifiedBy(a.clone()),
            Attestation·Custom { name, value } => Attestation·Custom {
                name: name.clone(),
                value: value.clone(),
            },
        }
    }
}

/// Agent role
//@ rune: derive(Clone)
☉ ᛈ Role {
    Admin,
    Operator,
    Agent,
    Observer,
    Custom(String),
}

/// Signed credential proving identity
☉ Σ Credential {
    /// Identity being attested
    ☉ identity_id: AgentId,

    /// Public key hash
    ☉ public_key_hash: [u8; 32],

    /// Attestations
    ☉ attestations: Vec<Attestation>,

    /// Issuer
    ☉ issuer: AuthorityId,

    /// Validity period
    ☉ valid_from: Timestamp,
    ☉ valid_until: Timestamp,

    /// Signature by issuer
    ☉ signature: Signature,
}

⊢ Credential {
    ☉ rite signable_bytes(&self) -> Vec<u8> {
        ≔ Δ bytes = Vec·new();
        bytes.extend(&self.identity_id.bytes);
        bytes.extend(&self.public_key_hash);
        bytes.extend(&self.issuer.bytes);
        bytes.extend(&self.valid_from.nanos.to_le_bytes());
        bytes.extend(&self.valid_until.nanos.to_le_bytes());
        bytes
    }
}

⊢ Clone ∀ Credential {
    rite clone(&self) -> Self {
        Self {
            identity_id: self.identity_id.clone(),
            public_key_hash: self.public_key_hash,
            attestations: self.attestations.clone(),
            issuer: self.issuer.clone(),
            valid_from: self.valid_from,
            valid_until: self.valid_until,
            signature: self.signature.clone(),
        }
    }
}

/// Verified identity result
☉ Σ VerifiedIdentity {
    ☉ id: AgentId,
    ☉ attestations: Vec<Attestation>,
    ☉ verified_at: Timestamp,
}

// ============================================================================
// Sandbox Types
// ============================================================================

/// Resource limits ∀ sandboxing
☉ Σ ResourceLimits {
    /// Maximum memory usage ∈ bytes
    ☉ max_memory: u64,

    /// Maximum CPU time
    ☉ max_cpu_time: Duration,

    /// Maximum file size ∈ bytes
    ☉ max_file_size: u64,

    /// Maximum network bytes
    ☉ max_network_bytes: u64,

    /// Maximum open files
    ☉ max_open_files: u32,

    /// Maximum processes/threads
    ☉ max_processes: u32,
}

⊢ ResourceLimits {
    ☉ rite default() -> Self {
        Self {
            max_memory: 256 * 1024 * 1024, // 256 MB
            max_cpu_time: Duration·seconds(60),
            max_file_size: 10 * 1024 * 1024, // 10 MB
            max_network_bytes: 100 * 1024 * 1024, // 100 MB
            max_open_files: 64,
            max_processes: 4,
        }
    }

    ☉ rite restrictive() -> Self {
        Self {
            max_memory: 64 * 1024 * 1024, // 64 MB
            max_cpu_time: Duration·seconds(10),
            max_file_size: 1024 * 1024, // 1 MB
            max_network_bytes: 1024 * 1024, // 1 MB
            max_open_files: 8,
            max_processes: 1,
        }
    }

    ☉ rite permissive() -> Self {
        Self {
            max_memory: 2 * 1024 * 1024 * 1024, // 2 GB
            max_cpu_time: Duration·minutes(30),
            max_file_size: 1024 * 1024 * 1024, // 1 GB
            max_network_bytes: 10 * 1024 * 1024 * 1024, // 10 GB
            max_open_files: 1024,
            max_processes: 64,
        }
    }
}

/// Isolation level ∀ sandboxing
//@ rune: derive(Copy, Clone)
☉ ᛈ IsolationLevel {
    /// Run ∈ same process (fast, less secure)
    Process,

    /// Run ∈ container (slower, more secure)
    Container,

    /// Run ∈ VM (slowest, most secure)
    VirtualMachine,
}

/// Syscall filter mode
//@ rune: derive(Copy, Clone)
☉ ᛈ FilterMode {
    /// Only listed syscalls allowed
    AllowList,
    /// Listed syscalls denied
    DenyList,
}

/// Network access policy
☉ ᛈ NetworkPolicy {
    /// No network access
    Deny,

    /// Specific hosts allowed
    AllowList(Vec<String>),

    /// Specific hosts denied
    DenyList(Vec<String>),

    /// All network allowed
    Allow,
}

⊢ Clone ∀ NetworkPolicy {
    rite clone(&self) -> Self {
        ⌥ self {
            NetworkPolicy·Deny => NetworkPolicy·Deny,
            NetworkPolicy·AllowList(v) => NetworkPolicy·AllowList(v.clone()),
            NetworkPolicy·DenyList(v) => NetworkPolicy·DenyList(v.clone()),
            NetworkPolicy·Allow => NetworkPolicy·Allow,
        }
    }
}

/// Filesystem access policy
☉ ᛈ FilesystemPolicy {
    /// No filesystem access
    Deny,

    /// Read-only access with optional write exceptions
    ReadOnly { write_exceptions: Vec<String> },

    /// Specific paths allowed
    AllowList(Vec<PathPattern>),

    /// Full access
    Allow,
}

⊢ Clone ∀ FilesystemPolicy {
    rite clone(&self) -> Self {
        ⌥ self {
            FilesystemPolicy·Deny => FilesystemPolicy·Deny,
            FilesystemPolicy·ReadOnly { write_exceptions } =>
                FilesystemPolicy·ReadOnly { write_exceptions: write_exceptions.clone() },
            FilesystemPolicy·AllowList(v) => FilesystemPolicy·AllowList(v.clone()),
            FilesystemPolicy·Allow => FilesystemPolicy·Allow,
        }
    }
}

/// Path pattern ∀ filesystem policy
//@ rune: derive(Clone)
☉ Σ PathPattern {
    ☉ pattern: String,
    ☉ permissions: PathPermissions,
}

/// Path permissions
//@ rune: derive(Copy, Clone)
☉ Σ PathPermissions {
    ☉ read: bool,
    ☉ write: bool,
    ☉ execute: bool,
}

/// Resource usage tracking
☉ Σ ResourceUsage {
    ☉ memory_bytes: u64,
    ☉ cpu_time: Duration,
    ☉ file_bytes_read: u64,
    ☉ file_bytes_written: u64,
    ☉ network_bytes_sent: u64,
    ☉ network_bytes_received: u64,
    ☉ open_files: u32,
    ☉ processes: u32,
}

⊢ ResourceUsage {
    ☉ rite zero() -> Self {
        Self {
            memory_bytes: 0,
            cpu_time: Duration·zero(),
            file_bytes_read: 0,
            file_bytes_written: 0,
            network_bytes_sent: 0,
            network_bytes_received: 0,
            open_files: 0,
            processes: 0,
        }
    }

    ☉ rite exceeds(&self, limits: &ResourceLimits) -> bool {
        self.memory_bytes > limits.max_memory
            || self.cpu_time.as_nanos() > limits.max_cpu_time.as_nanos()
            || self.file_bytes_written > limits.max_file_size
            || (self.network_bytes_sent + self.network_bytes_received) > limits.max_network_bytes
            || self.open_files > limits.max_open_files
            || self.processes > limits.max_processes
    }
}

// ============================================================================
// Memory Integrity Types
// ============================================================================

/// Memory identifier
☉ Σ MemoryId {
    ☉ bytes: [u8; 16],
}

⊢ MemoryId {
    ☉ rite new() -> Self {
        ≔ Δ bytes = [0u8; 16];
        getrandom(&Δ bytes);
        Self { bytes }
    }
}

⊢ Clone ∀ MemoryId {
    rite clone(&self) -> Self {
        Self { bytes: self.bytes }
    }
}

⊢ PartialEq ∀ MemoryId {
    rite eq(&self, other: &Self) -> bool {
        self.bytes == other.bytes
    }
}

⊢ Eq ∀ MemoryId {}

⊢ Hash ∀ MemoryId {
    rite hash<H: Hasher>(&self, state: &Δ H) {
        self.bytes.hash(state);
    }
}

/// Memory integrity status
☉ ᛈ IntegrityStatus {
    /// All memory verified
    Valid,

    /// Tampering detected
    Tampered { first_invalid: Option<MemoryId> },

    /// Unable to determine
    Unknown,
}

/// Memory type ∀ audit
//@ rune: derive(Clone)
☉ ᛈ MemoryType {
    Episodic,
    Semantic,
    Procedural,
    Working,
}

// ============================================================================
// Goal & Action Types
// ============================================================================

/// Goal identifier
☉ Σ GoalId {
    ☉ bytes: [u8; 16],
}

⊢ GoalId {
    ☉ rite new() -> Self {
        ≔ Δ bytes = [0u8; 16];
        getrandom(&Δ bytes);
        Self { bytes }
    }
}

⊢ Clone ∀ GoalId {
    rite clone(&self) -> Self {
        Self { bytes: self.bytes }
    }
}

⊢ PartialEq ∀ GoalId {
    rite eq(&self, other: &Self) -> bool {
        self.bytes == other.bytes
    }
}

⊢ Eq ∀ GoalId {}

⊢ Hash ∀ GoalId {
    rite hash<H: Hasher>(&self, state: &Δ H) {
        self.bytes.hash(state);
    }
}

/// Goal ∀ agents
//@ rune: derive(Clone)
☉ Σ Goal {
    ☉ id: GoalId,
    ☉ description: String,
    ☉ priority: u8,
    ☉ deadline: Option<Timestamp>,
    ☉ source: GoalSource,
}

⊢ Goal {
    ☉ rite new(description: &str) -> Self {
        Self {
            id: GoalId·new(),
            description: description.to_string(),
            priority: 5,
            deadline: None,
            source: GoalSource·SelfGenerated,
        }
    }

    ☉ rite with_priority(Δ self, priority: u8) -> Self {
        self.priority = priority;
        self
    }

    ☉ rite with_deadline(Δ self, deadline: Timestamp) -> Self {
        self.deadline = Some(deadline);
        self
    }

    ☉ rite from_source(Δ self, source: GoalSource) -> Self {
        self.source = source;
        self
    }
}

/// Source of a goal
//@ rune: derive(Clone)
☉ ᛈ GoalSource {
    /// From agent's constitution
    Constitution,

    /// From specific trusted agent
    Agent(AgentId),

    /// From authority
    Authority(AuthorityId),

    /// Self-generated
    SelfGenerated,
}

⊢ PartialEq ∀ GoalSource {
    rite eq(&self, other: &Self) -> bool {
        ⌥ (self, other) {
            (GoalSource·Constitution, GoalSource·Constitution) => true,
            (GoalSource·SelfGenerated, GoalSource·SelfGenerated) => true,
            (GoalSource·Agent(a), GoalSource·Agent(b)) => a == b,
            (GoalSource·Authority(a), GoalSource·Authority(b)) => a.bytes == b.bytes,
            _ => false,
        }
    }
}

/// Action ∀ agents
//@ rune: derive(Clone)
☉ Σ Action {
    ☉ id: ActionId,
    ☉ action_type: ActionType,
    ☉ params: Value,
    ☉ timestamp: Timestamp,
}

⊢ Action {
    ☉ rite name(&self) -> &str {
        ⌥ &self.action_type {
            ActionType·Tool { name, .. } => name,
            ActionType·Communicate { .. } => "communicate",
            ActionType·Think => "think",
            ActionType·Wait => "wait",
            ActionType·Terminate => "terminate",
            ActionType·Custom(name) => name,
        }
    }
}

/// Action identifier
☉ Σ ActionId {
    ☉ bytes: [u8; 16],
}

⊢ ActionId {
    ☉ rite new() -> Self {
        ≔ Δ bytes = [0u8; 16];
        getrandom(&Δ bytes);
        Self { bytes }
    }
}

⊢ Clone ∀ ActionId {
    rite clone(&self) -> Self {
        Self { bytes: self.bytes }
    }
}

/// Type of action
//@ rune: derive(Clone)
☉ ᛈ ActionType {
    Tool { name: String, params: Value },
    Communicate { to: AgentId, message: String },
    Think,
    Wait,
    Terminate,
    Custom(String),
}

/// Goal decision from guard
☉ ᛈ GoalDecision {
    Accepted,
    Rejected { reason: String },
    Suspicious { indicators: Vec<InjectionIndicator> },
}

/// Injection indicator ∀ goal protection
//@ rune: derive(Clone)
☉ Σ InjectionIndicator {
    ☉ pattern: String,
    ☉ location: String,
    ☉ severity: Severity,
    ☉ description: String,
}

/// Severity level
//@ rune: derive(Copy, Clone)
☉ ᛈ Severity {
    Low,
    Medium,
    High,
    Critical,
}

// ============================================================================
// Constitutional Types
// ============================================================================

/// Agent constitution
☉ Σ Constitution {
    /// Core directives that cannot be overridden
    ☉ directives: Vec<Directive>,

    /// Values that guide behavior
    ☉ values: Vec<String>,

    /// Emergency stop conditions
    ☉ emergency_conditions: Vec<String>,
}

⊢ Constitution {
    ☉ rite new() -> Self {
        Self {
            directives: vec![],
            values: vec![],
            emergency_conditions: vec![],
        }
    }

    ☉ rite default_safe() -> Self {
        Self {
            directives: vec![
                Directive {
                    name: "no_harm".to_string(),
                    description: "Never cainvoke harm to humans".to_string(),
                    constraint: DirectiveConstraint·Prohibition {
                        pattern: "harm|attack|damage|hurt".to_string(),
                    },
                },
                Directive {
                    name: "privacy".to_string(),
                    description: "Protect user privacy".to_string(),
                    constraint: DirectiveConstraint·Prohibition {
                        pattern: "exfiltrate|leak|expose.*private".to_string(),
                    },
                },
                Directive {
                    name: "honesty".to_string(),
                    description: "Be honest ∈ all communications".to_string(),
                    constraint: DirectiveConstraint·Requirement {
                        condition: "communication".to_string(),
                    },
                },
            ],
            values: vec![
                "Transparency about capabilities".to_string(),
                "Minimal footprint principle".to_string(),
                "Graceful degradation".to_string(),
            ],
            emergency_conditions: vec![
                "Emergency stop command received".to_string(),
                "Constitutional violation detected".to_string(),
                "Alignment drift exceeds threshold".to_string(),
            ],
        }
    }
}

⊢ Clone ∀ Constitution {
    rite clone(&self) -> Self {
        Self {
            directives: self.directives.clone(),
            values: self.values.clone(),
            emergency_conditions: self.emergency_conditions.clone(),
        }
    }
}

/// Constitutional directive
//@ rune: derive(Clone)
☉ Σ Directive {
    ☉ name: String,
    ☉ description: String,
    ☉ constraint: DirectiveConstraint,
}

⊢ Directive {
    ☉ rite prohibits(&self, goal: &Goal) -> bool {
        ⌥ &self.constraint {
            DirectiveConstraint·Prohibition { pattern } => {
                regex_match(pattern, &goal.description)
            }
            DirectiveConstraint·Requirement { .. } => false,
        }
    }
}

/// Directive constraint type
//@ rune: derive(Clone)
☉ ᛈ DirectiveConstraint {
    Prohibition { pattern: String },
    Requirement { condition: String },
}

/// Action decision from alignment monitor
☉ ᛈ ActionDecision {
    Allow,
    Block { reason: String },
    RequireConfirmation { reason: String },
}

// ============================================================================
// Audit Types
// ============================================================================

/// Audit entry identifier
☉ Σ AuditId {
    ☉ bytes: [u8; 16],
}

⊢ AuditId {
    ☉ rite new() -> Self {
        ≔ Δ bytes = [0u8; 16];
        getrandom(&Δ bytes);
        Self { bytes }
    }
}

⊢ Clone ∀ AuditId {
    rite clone(&self) -> Self {
        Self { bytes: self.bytes }
    }
}

/// Audit action types
☉ ᛈ AuditAction {
    // Lifecycle
    AgentStarted,
    AgentStopped { reason: String },

    // Goals
    GoalAdded { goal: Goal },
    GoalCompleted { goal_id: GoalId, outcome: Outcome },
    GoalFailed { goal_id: GoalId, reason: String },

    // Actions
    ActionTaken { action: Action },
    ToolInvoked { tool: String, params: Value },

    // Communication
    MessageSent { to: String, intent: String },
    MessageReceived { from: AgentId, intent: String },

    // Memory
    MemoryWritten { memory_type: MemoryType },
    MemoryRead { query: String },

    // Security
    AuthenticationAttempt { identity: AgentId, success: bool },
    AuthorizationCheck { action: String, allowed: bool },
    TamperDetected { location: String },
    AlertRaised { alert: Alert },

    // Custom
    Custom { name: String, data: Value },
}

⊢ Clone ∀ AuditAction {
    rite clone(&self) -> Self {
        ⌥ self {
            AuditAction·AgentStarted => AuditAction·AgentStarted,
            AuditAction·AgentStopped { reason } =>
                AuditAction·AgentStopped { reason: reason.clone() },
            AuditAction·GoalAdded { goal } =>
                AuditAction·GoalAdded { goal: goal.clone() },
            AuditAction·GoalCompleted { goal_id, outcome } =>
                AuditAction·GoalCompleted { goal_id: goal_id.clone(), outcome: outcome.clone() },
            AuditAction·GoalFailed { goal_id, reason } =>
                AuditAction·GoalFailed { goal_id: goal_id.clone(), reason: reason.clone() },
            AuditAction·ActionTaken { action } =>
                AuditAction·ActionTaken { action: action.clone() },
            AuditAction·ToolInvoked { tool, params } =>
                AuditAction·ToolInvoked { tool: tool.clone(), params: params.clone() },
            AuditAction·MessageSent { to, intent } =>
                AuditAction·MessageSent { to: to.clone(), intent: intent.clone() },
            AuditAction·MessageReceived { from, intent } =>
                AuditAction·MessageReceived { from: from.clone(), intent: intent.clone() },
            AuditAction·MemoryWritten { memory_type } =>
                AuditAction·MemoryWritten { memory_type: memory_type.clone() },
            AuditAction·MemoryRead { query } =>
                AuditAction·MemoryRead { query: query.clone() },
            AuditAction·AuthenticationAttempt { identity, success } =>
                AuditAction·AuthenticationAttempt { identity: identity.clone(), success: *success },
            AuditAction·AuthorizationCheck { action, allowed } =>
                AuditAction·AuthorizationCheck { action: action.clone(), allowed: *allowed },
            AuditAction·TamperDetected { location } =>
                AuditAction·TamperDetected { location: location.clone() },
            AuditAction·AlertRaised { alert } =>
                AuditAction·AlertRaised { alert: alert.clone() },
            AuditAction·Custom { name, data } =>
                AuditAction·Custom { name: name.clone(), data: data.clone() },
        }
    }
}

/// Audit outcome
//@ rune: derive(Clone)
☉ ᛈ AuditOutcome {
    Success,
    Failure { error: String },
    Blocked { reason: String },
    Partial { details: String },
}

/// Action outcome
//@ rune: derive(Clone)
☉ ᛈ Outcome {
    Success { result: Value },
    Failure { error: String },
    Partial { progress: f32, details: String },
}

/// Security alert
//@ rune: derive(Clone)
☉ ᛈ Alert {
    /// Low compliance score
    LowCompliance { action: Action, score: f32 },

    /// Behavioral drift detected
    BehavioralDrift { drift: f32, threshold: f32 },

    /// Tamper detected
    TamperDetected { location: String, details: String },

    /// Authentication failure
    AuthenticationFailure { identity: AgentId, reason: String },

    /// Injection attempt
    InjectionAttempt { source: String, indicators: Vec<InjectionIndicator> },

    /// Resource limit approaching
    ResourceWarning { resource: String, usage: f32, limit: f32 },

    /// Custom alert
    Custom { name: String, data: Value },
}

// ============================================================================
// Compliance Types
// ============================================================================

/// Compliance check result
☉ Σ ComplianceResult {
    ☉ compliant: bool,
    ☉ score: f32,
    ☉ violations: Vec<DirectiveResult>,
}

/// Result of checking a single directive
☉ Σ DirectiveResult {
    ☉ directive: Directive,
    ☉ compliant: bool,
    ☉ score: f32,
    ☉ explanation: Option<String>,
}

// ============================================================================
// Collective Defense Types
// ============================================================================

/// Knowledge identifier
☉ Σ KnowledgeId {
    ☉ bytes: [u8; 16],
}

⊢ KnowledgeId {
    ☉ rite new() -> Self {
        ≔ Δ bytes = [0u8; 16];
        getrandom(&Δ bytes);
        Self { bytes }
    }
}

⊢ Clone ∀ KnowledgeId {
    rite clone(&self) -> Self {
        Self { bytes: self.bytes }
    }
}

⊢ PartialEq ∀ KnowledgeId {
    rite eq(&self, other: &Self) -> bool {
        self.bytes == other.bytes
    }
}

⊢ Eq ∀ KnowledgeId {}

⊢ Hash ∀ KnowledgeId {
    rite hash<H: Hasher>(&self, state: &Δ H) {
        self.bytes.hash(state);
    }
}

/// Knowledge item
//@ rune: derive(Clone)
☉ Σ Knowledge {
    ☉ id: KnowledgeId,
    ☉ content: Value,
    ☉ source: AgentId,
    ☉ timestamp: Timestamp,
    ☉ confidence: f32,
}

/// Verification result ∀ collective defense
☉ ᛈ Verification {
    Trusted { chain: ProvenanceChain },
    Suspicious { reasons: Vec<String> },
    Rejected { reasons: Vec<String> },
}

/// Provenance chain ∀ knowledge
☉ Σ ProvenanceChain {
    ☉ links: Vec<ProvenanceLink>,
    ☉ valid: bool,
}

⊢ ProvenanceChain {
    ☉ rite new() -> Self {
        Self {
            links: vec![],
            valid: true,
        }
    }

    ☉ rite is_valid(&self) -> bool {
        self.valid
    }
}

/// Link ∈ provenance chain
☉ Σ ProvenanceLink {
    ☉ from: AgentId,
    ☉ to: AgentId,
    ☉ timestamp: Timestamp,
    ☉ transformation: Option<String>,
}

/// Accuracy history ∀ reputation
☉ Σ AccuracyHistory {
    total_claims: u64,
    verified_claims: u64,
    recent_scores: VecDeque<f32>,
}

⊢ AccuracyHistory {
    ☉ rite new() -> Self {
        Self {
            total_claims: 0,
            verified_claims: 0,
            recent_scores: VecDeque·with_capacity(100),
        }
    }

    ☉ rite record(&Δ self, accurate: bool) {
        self.total_claims += 1;
        ⎇ accurate {
            self.verified_claims += 1;
        }

        ≔ score = ⎇ accurate { 1.0 } ⎉ { 0.0 };
        ⎇ self.recent_scores.len() >= 100 {
            self.recent_scores.pop_front();
        }
        self.recent_scores.push_back(score);
    }

    ☉ rite score(&self) -> f32 {
        ⎇ self.total_claims == 0 {
            ⤺ 0.5;
        }

        // Weight recent accuracy more heavily
        ≔ overall = self.verified_claims as f32 / self.total_claims as f32;
        ≔ recent: f32 = ⎇ self.recent_scores.is_empty() {
            0.5
        } ⎉ {
            self.recent_scores.iter().sum·<f32>() / self.recent_scores.len() as f32
        };

        overall * 0.3 + recent * 0.7
    }
}

// ============================================================================
// Common Types
// ============================================================================

/// Timestamp
//@ rune: derive(Copy, Clone, PartialOrd, Ord, PartialEq, Eq)
☉ Σ Timestamp {
    ☉ nanos: u64,
}

⊢ Timestamp {
    ☉ rite now() -> Self {
        Self { nanos: system_time_nanos() }
    }

    ☉ rite zero() -> Self {
        Self { nanos: 0 }
    }

    ☉ rite from_nanos(nanos: u64) -> Self {
        Self { nanos }
    }
}

/// Duration
☉ Σ Duration {
    nanos: u64,
}

⊢ Duration {
    ☉ rite zero() -> Self { Self { nanos: 0 } }
    ☉ rite seconds(s: u64) -> Self { Self { nanos: s * 1_000_000_000 } }
    ☉ rite minutes(m: u64) -> Self { Self { nanos: m * 60_000_000_000 } }
    ☉ rite hours(h: u64) -> Self { Self { nanos: h * 3_600_000_000_000 } }

    ☉ rite as_nanos(&self) -> u64 { self.nanos }
}

⊢ Copy ∀ Duration {}
⊢ Clone ∀ Duration {
    rite clone(&self) -> Self { *self }
}

/// Generic value type
//@ rune: derive(Clone)
☉ Σ Value {
    inner: Vec<u8>,
}

⊢ Value {
    ☉ rite empty() -> Self { Self { inner: vec![] } }
    ☉ rite string(s: &str) -> Self { Self { inner: s.as_bytes().to_vec() } }
    ☉ rite bytes(b: Vec<u8>) -> Self { Self { inner: b } }

    ☉ rite as_string(&self) -> Option<String> {
        String·from_utf8(self.inner.clone()).ok()
    }

    ☉ rite as_agent_id(&self) -> Option<AgentId> {
        ⎇ self.inner.len() == 32 {
            ≔ Δ bytes = [0u8; 32];
            bytes.copy_from_slice(&self.inner);
            Some(AgentId { bytes })
        } ⎉ {
            None
        }
    }
}

// ============================================================================
// Error Types
// ============================================================================

/// Cryptographic error
☉ ᛈ CryptoError {
    InvalidKey,
    InvalidSignature,
    InvalidData,
    EncryptionFailed,
    DecryptionFailed,
}

/// Verification error
☉ ᛈ VerificationError {
    Revoked,
    Expired,
    UntrustedIssuer,
    InvalidSignature,
    InvalidCredential,
}

/// Sandbox error
☉ ᛈ SandboxError {
    Timeout,
    ResourceLimitExceeded(ResourceUsage),
    SyscallDenied(String),
    NetworkDenied(String),
    FilesystemDenied(String),
    ExecutionFailed(String),
}

/// Memory error
☉ ᛈ MemoryError {
    IntegrityViolation,
    EncryptionError(CryptoError),
    NotFound,
    AccessDenied,
}

/// Channel error
☉ ᛈ ChannelError {
    InvalidSignature,
    ReplayDetected,
    KeyExchangeFailed,
    EncryptionFailed,
    DecryptionFailed,
}

/// Aegis error
☉ ᛈ AegisError {
    IdentityError(VerificationError),
    SandboxError(SandboxError),
    MemoryError(MemoryError),
    ChannelError(ChannelError),
    ActionBlocked(String),
    AlignmentViolation(String),
    CryptoError(CryptoError),
}

// ============================================================================
// Configuration Types
// ============================================================================

/// Security level presets
//@ rune: derive(Copy, Clone)
☉ ᛈ SecurityLevel {
    /// Development: minimal security, maximum capability
    Development,

    /// Standard: balanced security ∀ production
    Standard,

    /// Hardened: prioritize security ∀ sensitive operations
    Hardened,

    /// Paranoid: maximum security, adversarial environment
    Paranoid,
}

/// Aegis configuration
☉ Σ AegisConfig {
    /// Security level
    ☉ level: SecurityLevel,

    /// Resource limits
    ☉ limits: ResourceLimits,

    /// Isolation level
    ☉ isolation: IsolationLevel,

    /// Constitution
    ☉ constitution: Constitution,

    /// Drift threshold ∀ alignment monitoring
    ☉ drift_threshold: f32,

    /// Compliance threshold
    ☉ compliance_threshold: f32,

    /// Enable audit logging
    ☉ audit_enabled: bool,

    /// Enable memory encryption
    ☉ memory_encryption: bool,
}

⊢ AegisConfig {
    ☉ rite development() -> Self {
        Self {
            level: SecurityLevel·Development,
            limits: ResourceLimits·permissive(),
            isolation: IsolationLevel·Process,
            constitution: Constitution·new(),
            drift_threshold: 0.5,
            compliance_threshold: 0.3,
            audit_enabled: false,
            memory_encryption: false,
        }
    }

    ☉ rite standard() -> Self {
        Self {
            level: SecurityLevel·Standard,
            limits: ResourceLimits·default(),
            isolation: IsolationLevel·Process,
            constitution: Constitution·default_safe(),
            drift_threshold: 0.3,
            compliance_threshold: 0.7,
            audit_enabled: true,
            memory_encryption: false,
        }
    }

    ☉ rite hardened() -> Self {
        Self {
            level: SecurityLevel·Hardened,
            limits: ResourceLimits·restrictive(),
            isolation: IsolationLevel·Container,
            constitution: Constitution·default_safe(),
            drift_threshold: 0.2,
            compliance_threshold: 0.9,
            audit_enabled: true,
            memory_encryption: true,
        }
    }

    ☉ rite paranoid() -> Self {
        Self {
            level: SecurityLevel·Paranoid,
            limits: ResourceLimits·restrictive(),
            isolation: IsolationLevel·VirtualMachine,
            constitution: Constitution·default_safe(),
            drift_threshold: 0.1,
            compliance_threshold: 0.95,
            audit_enabled: true,
            memory_encryption: true,
        }
    }
}

// ============================================================================
// External Function Declarations (would be provided by runtime)
// ============================================================================

// extern rite getrandom(buf);
// extern rite system_time_nanos();
// extern rite ed25519_keygen(seed);
// extern rite ed25519_sign(private_key, data);
// extern rite ed25519_verify(public_key, data, signature);
// extern rite aes_gcm_encrypt(key, nonce, plaintext, aad);
// extern rite aes_gcm_decrypt(key, nonce, ciphertext, aad);
// extern rite hkdf_derive(input, info);
// extern rite derive_nonce(counter);
// extern rite sha256(data);
// extern rite regex_match(pattern, text);

// Use declarations ∀ std traits
invoke std·hash·{Hash, Hasher};
